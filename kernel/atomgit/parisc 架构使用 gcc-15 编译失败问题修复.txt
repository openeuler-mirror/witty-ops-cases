#标题
parisc 架构使用 gcc-15 编译失败问题修复

#内核版本
OLK-6.6

#问题现象
在使用 gcc-15 编译器编译 parisc 架构内核的解压缩程序时，编译过程报错中断。

#主要错误日志
In file included from include/uapi/linux/posix_types.h:5,
                 from arch/parisc/boot/compressed/misc.c:7:
include/linux/stddef.h:11:9: error: cannot use keyword 'false' as enumeration constant
   11 |         false   = 0,

#问题根因
gcc-15 版本将默认 C 方言变更为 gnu23，导致内核自定义的 bool 类型定义（将 `false` 定义为枚举常量）与 gnu23 标准中的关键字 `false` 发生冲突，从而引发编译错误。

#解决方案
在 arch/parisc/boot/compressed/Makefile 中显式指定编译选项 `-std=gnu11`，强制使用 gnu11 标准，使其与其他架构保持一致，避免方言变更导致的语法冲突。

#来源
https://atomgit.com/openeuler/kernel/commit/69e24d5c9528826a8daae8db0cba076966f90131