#标题
[编译] 使用 clang 编译内核时 mutex.c 出现变量未初始化警告

#内核版本
OLK-6.6

#问题现象
使用 clang 编译内核时，编译器报错提示变量 `ifs_clock` 可能未初始化即被使用，导致编译过程产生警告。

#主要错误日志
```text
kernel/locking/mutex.c:725:24: note: uninitialized use occurs here
  725 |         cgroup_ifs_leave_lock(ifs_clock, IFS_MUTEX);
        |                               ^~~~~~~~~
kernel/locking/mutex.c:576:15: note: initialize the variable 'ifs_clock' to silence this warning
  576 |         u64 ifs_clock;
        |                      ^
        |                       = 0
2 warnings generated.
```

#问题根因
在 `kernel/locking/mutex.c` 的 `__mutex_lock_common` 函数中，变量 `ifs_clock` 定义时未进行初始化。在某些代码路径下（如 `__mutex_trylock` 成功跳转），`ifs_clock` 未被赋值即被传递给 `cgroup_ifs_leave_lock` 函数，导致编译器检测到未初始化使用风险并报出警告。

#解决方案
1. 在 `kernel/locking/mutex.c` 中将 `u64 ifs_clock;` 修改为 `u64 ifs_clock = 0;`，显式初始化变量以消除编译警告。
2. 在 `include/linux/cgroup.h` 的 `cgroup_ifs_leave_lock` 函数中增加判断逻辑 `if (unlikely(!clock)) return;`，确保当初值传入时直接返回，保证逻辑安全性。

#来源
https://atomgit.com/openeuler/kernel/commit/60fb74c8564334b664cace1bcd7bb4f8e1ed7101