#标题
PMT Discovery 驱动在 32 位架构下编译报错

#内核版本
OLK-6.6

#问题现象
在 i386 32 位架构下使用 allmodconfig 编译内核时，编译过程因格式字符串类型不匹配报错并终止（开启 -Werror=format），导致驱动无法编译通过。

#主要错误日志
- drivers/platform/x86/intel/pmt/discovery.c:427:35: error: format '%ld' expects argument of type 'long int', but argument 2 has type 'size_t' {aka 'unsigned int'} [-Werror=format=]
- drivers/platform/x86/intel/pmt/discovery-kunit.c: In function 'validate_pmt_regions':
- include/linux/kern_levels.h:5:25: error: format '%lu' expects argument of type 'long unsigned int', but argument 4 has type 'size_t' {aka 'unsigned int'} [-Werror=format=]

#问题根因
代码中错误地使用了 `%ld` 和 `%lu` 格式说明符来打印 `size_t` 类型的变量。在 64 位平台上 `size_t` 是 `unsigned long`，但在 32 位平台上 `size_t` 是 `unsigned int`，导致在 32 位架构下编译时出现类型不匹配的格式错误。

#解决方案
将 `discovery.c` 和 `discovery-kunit.c` 中用于打印 `size_t` 变量的格式说明符统一修改为 `%zu`，该说明符专门用于 `size_t` 类型，可同时兼容 32 位和 64 位架构。

#来源
https://atomgit.com/openeuler/kernel/commit/d5b121a0c92d7d197b8ac496d8f8412607ee8429