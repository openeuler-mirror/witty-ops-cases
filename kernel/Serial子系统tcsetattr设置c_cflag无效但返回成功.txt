#标题
Serial子系统tcsetattr设置c_cflag无效但返回成功

#内核版本
3.19.2

#问题现象
LSB测试用例失败。在伪终端上调用 `tcsetattr` 尝试设置 `c_cflag`（如 `PARENB` 标志）时，系统调用返回成功（0），但随后通过 `tcgetattr` 读取发现设置并未生效。LSB预期结果应为返回错误 `EINVAL` 或设置成功。

#问题根因
未明确。外部引用指出这是内核 Bug，但本报告未提供具体的代码逻辑分析。

#解决方案
暂无。Bug 状态为 NEW。注：glibc 中存在相关 patch 可规避此问题。
