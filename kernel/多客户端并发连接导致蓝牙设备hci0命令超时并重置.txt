#标题
多客户端并发连接导致蓝牙设备hci0命令超时并重置

#内核版本
4.14.79

#问题现象
- 当已有客户端连接时，若新客户端尝试连接或执行 gattool 操作，hci0 设备会消失随后重新枚举。
- 服务器应用程序因设备丢失而崩溃。
- 内核日志显示 "Bluetooth: hci0 command 0x200c tx timeout"。
- 日志记录 USB 设备断开连接后立即重新连接。

#问题根因
内核日志显示 hci0 发送命令 0x200c 超时，导致设备被 USB 子系统重置。具体为何多客户端并发会触发此超时，报告中未明确分析结论。

#解决方案
暂无
