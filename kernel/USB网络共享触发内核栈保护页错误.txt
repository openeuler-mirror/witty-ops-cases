#标题
USB网络共享触发内核栈保护页错误

#内核版本
4.9.0-3-amd64

#问题现象
- 连接手机开启USB网络共享后，执行 `sudo dhclient` 命令获取IP地址时触发内核错误。
- 系统日志显示：`BUG: stack guard page was hit at ffffc046424501c0 (stack is ffffc0464244c000..ffffc0464244ffff)`。
- 该问题在 4.8.0 版本内核上未复现。

#问题根因
未明确

#解决方案
暂无
