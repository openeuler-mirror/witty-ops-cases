#标题
MCP251X驱动持有互斥锁调用free_irq触发死锁

#内核版本
未明确

#问题现象
系统在打开 MCP251X 设备（调用 mcp251x_open）时发生死锁，导致相关进程挂起或系统无响应。

#问题根因
MCP251X 驱动在 `mcp251x_set_normal_mode` 函数失败时的错误处理路径中，未释放 `mpc_lock` 互斥锁即调用 `free_irq()`。若此时中断已经发生，`free_irq()` 会阻塞等待中断处理程序完成；而中断处理程序试图获取 `mpc_lock` 互斥锁，从而形成死锁。

#解决方案
暂无
