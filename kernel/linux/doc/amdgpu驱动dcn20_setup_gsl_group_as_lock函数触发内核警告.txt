#标题
amdgpu驱动dcn20_setup_gsl_group_as_lock函数触发内核警告

#内核版本
5.3.0-26-generic

#问题现象
系统日志中记录内核 WARNING 堆栈信息，具体报错位置为 `drivers/gpu/drm/amd/amdgpu/../display/dc/dcn20/dcn20_hwseq.c:1961`，触发函数为 `dcn20_setup_gsl_group_as_lock`。调用栈显示问题发生在 `commit_work` -> `amdgpu_dm_atomic_commit_tail` -> `dc_commit_updates_for_stream` 的执行路径中。

#问题根因
未明确。日志显示在 amdgpu 显示核心模块的 `dcn20_setup_gsl_group_as_lock` 函数中触发了断言失败（WARNING），具体触发逻辑需结合源码分析，提供的 JSON 数据中未包含根因分析。

#解决方案
暂无
