#标题
使用SCHED_DEADLINE运行周期任务导致系统冻结

#内核版本
5.4/preempt-rt

#问题现象
- 系统运行过程中发生冻结。
- 故障场景为使用 SCHED_DEADLINE 调度策略执行一个 5ms 的周期性任务，该任务负责对串口设备进行读写操作，并使用了 pthread_rwlock 机制。

#问题根因
未明确（报告中提及包含 Kernel call trace，但提供的 JSON 数据中未包含具体的堆栈信息，无法分析具体的死锁点或崩溃原因）。

#解决方案
暂无（Bug 状态为 NEW，尚未修复）。
