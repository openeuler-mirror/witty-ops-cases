#标题
Btrfs事务线程与Postgres进程发生死锁

#内核版本
5.2.4

#问题现象
- 系统发生死锁，涉及 `btrfs-transacti` 和 `postgres` 进程
- `btrfs-transacti` 进程处于 D 状态（不可中断睡眠），阻塞在 `lock_extent_buffer_for_io` 函数
- 监控显示单个 CPU 核心 iowait 持续为 100%，系统整体负载异常

#问题根因
未明确（报告中仅观察到死锁现象及阻塞点，未提供具体的代码逻辑或锁依赖分析）

#解决方案
暂无（Bug 状态为 NEW，尚未修复）
