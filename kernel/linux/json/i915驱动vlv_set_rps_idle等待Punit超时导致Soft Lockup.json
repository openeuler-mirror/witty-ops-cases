{
  "title": "i915驱动vlv_set_rps_idle等待Punit超时导致Soft Lockup",
  "kernel_version": "3.16.7-ckt2-1",
  "phenomenon": "- 系统运行一段时间后鼠标卡死，系统挂起无响应。\n- 内核日志报错：`[drm:vlv_set_rps_idle] *ERROR* timed out waiting for Punit`。\n- NMI watchdog 检测到 CPU#0 发生 soft lockup，进程卡在 `futex_wait`。\n- Call Trace 包含：`_raw_spin_lock`, `futex_wait_setup`, `futex_wait`, `do_futex`。",
  "root_cause": "在 `drivers/gpu/drm/i915/intel_pm.c` 文件的 `vlv_set_rps_idle` 函数中，针对 ValleyView 平台（如 Intel J1800 CPU）的条件判断逻辑存在缺陷。原代码逻辑仅针对 CherryView 或特定修订版（revision >= 0xd）的硬件跳过 gfx clock 强制操作，导致 ValleyView 平台在设置 RPS idle 状态时等待 Punit 响应超时，进而引发 soft lockup。",
  "solution": "开发者建议修改 `vlv_set_rps_idle` 函数中的判断条件，将 `if (IS_CHERRYVIEW(dev) || dev->pdev->revision >= 0xd)` 修改为 `if (IS_CHERRYVIEW(dev) || IS_VALLEYVIEW(dev))`。Bug 状态为 NEW，暂无明确的内核主线合并记录。",
  "category": "kernel",
  "source_file": "i915驱动vlv_set_rps_idle等待Punit超时导致Soft Lockup.txt",
  "content": "内核版本：3.16.7-ckt2-1\n\n## 问题现象\n- 系统运行一段时间后鼠标卡死，系统挂起无响应。\n- 内核日志报错：`[drm:vlv_set_rps_idle] *ERROR* timed out waiting for Punit`。\n- NMI watchdog 检测到 CPU#0 发生 soft lockup，进程卡在 `futex_wait`。\n- Call Trace 包含：`_raw_spin_lock`, `futex_wait_setup`, `futex_wait`, `do_futex`。\n\n## 问题根因\n在 `drivers/gpu/drm/i915/intel_pm.c` 文件的 `vlv_set_rps_idle` 函数中，针对 ValleyView 平台（如 Intel J1800 CPU）的条件判断逻辑存在缺陷。原代码逻辑仅针对 CherryView 或特定修订版（revision >= 0xd）的硬件跳过 gfx clock 强制操作，导致 ValleyView 平台在设置 RPS idle 状态时等待 Punit 响应超时，进而引发 soft lockup。\n\n## 解决方案\n开发者建议修改 `vlv_set_rps_idle` 函数中的判断条件，将 `if (IS_CHERRYVIEW(dev) || dev->pdev->revision >= 0xd)` 修改为 `if (IS_CHERRYVIEW(dev) || IS_VALLEYVIEW(dev))`。Bug 状态为 NEW，暂无明确的内核主线合并记录。"
}
