{
  "title": "KVM启用xsave特性后返回错误CPUID数据导致FreeBSD客户机崩溃",
  "kernel_version": "3.2.0-37",
  "phenomenon": "在KVM虚拟机中使用 `-cpu core2duo,+xsave` 参数启动FreeBSD 9.1时，系统在loader加载后立即崩溃，报错信息为 \"CPU0 does not support X87 or SSE2 : 0\"。若不启用xsave指令集，系统可正常启动。",
  "root_cause": "KVM在启用 `xsave` 特性时，向客户机返回了错误的CPUID数据，未能正确报告X87或SSE2特性的存在，违反了Unix amd64 ABI要求，导致FreeBSD内核检测到CPU不支持必需的浮点特性而触发崩溃。",
  "solution": "该问题已在后续QEMU/KVM版本中修复（参考2014年2月QEMU开发邮件列表相关补丁），升级虚拟化组件版本即可解决。",
  "category": "kernel",
  "source_file": "KVM启用xsave特性后返回错误CPUID数据导致FreeBSD客户机崩溃.txt",
  "content": "内核版本：3.2.0-37\n\n## 问题现象\n在KVM虚拟机中使用 `-cpu core2duo,+xsave` 参数启动FreeBSD 9.1时，系统在loader加载后立即崩溃，报错信息为 \"CPU0 does not support X87 or SSE2 : 0\"。若不启用xsave指令集，系统可正常启动。\n\n## 问题根因\nKVM在启用 `xsave` 特性时，向客户机返回了错误的CPUID数据，未能正确报告X87或SSE2特性的存在，违反了Unix amd64 ABI要求，导致FreeBSD内核检测到CPU不支持必需的浮点特性而触发崩溃。\n\n## 解决方案\n该问题已在后续QEMU/KVM版本中修复（参考2014年2月QEMU开发邮件列表相关补丁），升级虚拟化组件版本即可解决。"
}
