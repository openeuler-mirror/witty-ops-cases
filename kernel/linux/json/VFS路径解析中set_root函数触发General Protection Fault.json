{
  "title": "VFS路径解析中set_root函数触发General Protection Fault",
  "kernel_version": "v5.2-rc6",
  "phenomenon": "- 内核触发 general protection fault：0000 [#1]，导致进程崩溃。\n- KASAN 报告提示可能由空指针解引用或用户内存访问引起。\n- 故障发生位置：RIP 指向 `set_root+0x183/0x440` (fs/namei.c:818)，调用栈显示路径为 `path_init` -> `path_lookupat` -> `filename_lookup` -> `vfs_statx` -> `__do_sys_newstat`。\n- 故障场景由 Syzkaller 通过 SCSI ioctl 测试触发。",
  "root_cause": "在 `path_init` 函数进行路径查找初始化时，`set_root` 函数内部调用 `__read_seqcount_begin` 读取序列锁时访问了非法内存地址（寄存器 RAX 为 0），导致 General Protection Fault。具体触发条件与 Syzkaller 构造的特定 SCSI 命令发送操作相关。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "VFS路径解析中set_root函数触发General Protection Fault.txt",
  "content": "内核版本：v5.2-rc6\n\n## 问题现象\n- 内核触发 general protection fault：0000 [#1]，导致进程崩溃。\n- KASAN 报告提示可能由空指针解引用或用户内存访问引起。\n- 故障发生位置：RIP 指向 `set_root+0x183/0x440` (fs/namei.c:818)，调用栈显示路径为 `path_init` -> `path_lookupat` -> `filename_lookup` -> `vfs_statx` -> `__do_sys_newstat`。\n- 故障场景由 Syzkaller 通过 SCSI ioctl 测试触发。\n\n## 问题根因\n在 `path_init` 函数进行路径查找初始化时，`set_root` 函数内部调用 `__read_seqcount_begin` 读取序列锁时访问了非法内存地址（寄存器 RAX 为 0），导致 General Protection Fault。具体触发条件与 Syzkaller 构造的特定 SCSI 命令发送操作相关。\n\n## 解决方案\n暂无"
}
