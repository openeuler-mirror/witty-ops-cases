{
  "title": "e1000驱动发送数据时writel操作导致Soft Lockup",
  "kernel_version": "5.10",
  "phenomenon": "- 系统触发 soft lockup，日志显示：`watchdog: BUG: soft lockup - CPU#3 stuck for 115s! [sshd:1029]`\n- 故障调用栈显示进程卡在 `e1000_xmit_frame` 函数的 `writel` 操作处（`drivers/net/ethernet/intel/e1000/e1000_main.c:3274`）。\n- 触发路径为 `sshd` 进程执行 TCP 发送数据流程（`tcp_sendmsg` -> `ip_queue_xmit` -> `dev_hard_start_xmit`）。",
  "root_cause": "未明确（报告仅提供了故障现场堆栈，未包含对根因的具体分析）。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "e1000驱动发送数据时writel操作导致Soft Lockup.txt",
  "content": "内核版本：5.10\n\n## 问题现象\n- 系统触发 soft lockup，日志显示：`watchdog: BUG: soft lockup - CPU#3 stuck for 115s! [sshd:1029]`\n- 故障调用栈显示进程卡在 `e1000_xmit_frame` 函数的 `writel` 操作处（`drivers/net/ethernet/intel/e1000/e1000_main.c:3274`）。\n- 触发路径为 `sshd` 进程执行 TCP 发送数据流程（`tcp_sendmsg` -> `ip_queue_xmit` -> `dev_hard_start_xmit`）。\n\n## 问题根因\n未明确（报告仅提供了故障现场堆栈，未包含对根因的具体分析）。\n\n## 解决方案\n暂无"
}
