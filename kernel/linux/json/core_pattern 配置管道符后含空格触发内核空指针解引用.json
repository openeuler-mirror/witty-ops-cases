{
  "title": "core_pattern 配置管道符后含空格触发内核空指针解引用",
  "kernel_version": "5.4.28",
  "phenomenon": "- 系统日志报错 \"BUG: kernel NULL pointer dereference, address: 0000000000000020\"。\n- 触发 Oops，RIP 位于 `do_coredump+0x5b4/0x1080`。\n- 当 `/proc/sys/kernel/core_pattern` 内容为 \"| /root/coredump.sh\"（管道符后有空格）时，进程（如 cat）触发 core dump 会导致内核崩溃。",
  "root_cause": "在 `do_coredump` 函数处理逻辑中，当 core_pattern 字符串以管道符 \"|\" 开头且后跟空格时，解析过程触发空指针解引用。用户推测此问题为回归问题，可能与 lkml 上关于 core_pattern 解析的补丁有关。",
  "solution": "暂无官方补丁。规避措施：配置 core_pattern 时确保管道符 \"|\" 后无空格，例如使用 \"|/root/coredump.sh\"。",
  "category": "kernel",
  "source_file": "core_pattern 配置管道符后含空格触发内核空指针解引用.txt",
  "content": "内核版本：5.4.28\n\n## 问题现象\n- 系统日志报错 \"BUG: kernel NULL pointer dereference, address: 0000000000000020\"。\n- 触发 Oops，RIP 位于 `do_coredump+0x5b4/0x1080`。\n- 当 `/proc/sys/kernel/core_pattern` 内容为 \"| /root/coredump.sh\"（管道符后有空格）时，进程（如 cat）触发 core dump 会导致内核崩溃。\n\n## 问题根因\n在 `do_coredump` 函数处理逻辑中，当 core_pattern 字符串以管道符 \"|\" 开头且后跟空格时，解析过程触发空指针解引用。用户推测此问题为回归问题，可能与 lkml 上关于 core_pattern 解析的补丁有关。\n\n## 解决方案\n暂无官方补丁。规避措施：配置 core_pattern 时确保管道符 \"|\" 后无空格，例如使用 \"|/root/coredump.sh\"。"
}
