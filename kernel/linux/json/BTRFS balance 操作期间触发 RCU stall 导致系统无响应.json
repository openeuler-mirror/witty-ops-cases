{
  "title": "BTRFS balance 操作期间触发 RCU stall 导致系统无响应",
  "kernel_version": "6.1.72-mcbin",
  "phenomenon": "- 系统日志报告 RCU stall：`rcu: INFO: rcu_sched self-detected stall on CPU`\n- 进程 `btrfs` (PID 1336962) 卡死，调用栈显示进程处于 `down_write` 状态\n- 调用链路：`btrfs_balance` -> `btrfs_relocate_chunk` -> `clean_dirty_subvols` -> `btrfs_drop_snapshot` -> `walk_down_tree` -> `do_walk_down` -> `btrfs_tree_lock` -> `down_write`",
  "root_cause": "进程在执行 BTRFS balance 相关的清理脏子卷操作时，尝试获取写锁（`down_write`）阻塞过久，导致 RCU 调度器检测到 CPU 停顿；具体死锁或阻塞原因未明确。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "BTRFS balance 操作期间触发 RCU stall 导致系统无响应.txt",
  "content": "内核版本：6.1.72-mcbin\n\n## 问题现象\n- 系统日志报告 RCU stall：`rcu: INFO: rcu_sched self-detected stall on CPU`\n- 进程 `btrfs` (PID 1336962) 卡死，调用栈显示进程处于 `down_write` 状态\n- 调用链路：`btrfs_balance` -> `btrfs_relocate_chunk` -> `clean_dirty_subvols` -> `btrfs_drop_snapshot` -> `walk_down_tree` -> `do_walk_down` -> `btrfs_tree_lock` -> `down_write`\n\n## 问题根因\n进程在执行 BTRFS balance 相关的清理脏子卷操作时，尝试获取写锁（`down_write`）阻塞过久，导致 RCU 调度器检测到 CPU 停顿；具体死锁或阻塞原因未明确。\n\n## 解决方案\n暂无"
}
