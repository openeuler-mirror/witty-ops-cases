{
  "title": "bcache 分离缓存设备时引用计数检查失败导致 Kernel BUG",
  "kernel_version": "4.14.114",
  "phenomenon": "- 手动执行 bcache 缓存设备分离操作时触发 Kernel BUG。\n- 系统日志显示 \"invalid opcode: 0000 [#1]\"，堆栈跟踪指向 `cached_dev_detach_finish` 函数。\n- 触发条件为 `BUG_ON(atomic_read(&dc->count))`。",
  "root_cause": "在 `cached_dev_detach_finish` 函数中，代码断言 `dc->count` 引用计数必须为零。当分离过程中存在未完成的 I/O 操作或 I/O 错误未被正确检测处理时，引用计数未归零，导致断言失败并触发 Kernel BUG。",
  "solution": "用户通过回溯移植主线内核补丁修复了该问题，主要涉及 `bcache_4.15_backport_writeback.patch` 等补丁集。修复后执行分离操作前，需先执行 `echo none > /sys/block/bcacheX/bcache/cache_module`。",
  "category": "kernel",
  "source_file": "bcache 分离缓存设备时引用计数检查失败导致 Kernel BUG.txt",
  "content": "内核版本：4.14.114\n\n## 问题现象\n- 手动执行 bcache 缓存设备分离操作时触发 Kernel BUG。\n- 系统日志显示 \"invalid opcode: 0000 [#1]\"，堆栈跟踪指向 `cached_dev_detach_finish` 函数。\n- 触发条件为 `BUG_ON(atomic_read(&dc->count))`。\n\n## 问题根因\n在 `cached_dev_detach_finish` 函数中，代码断言 `dc->count` 引用计数必须为零。当分离过程中存在未完成的 I/O 操作或 I/O 错误未被正确检测处理时，引用计数未归零，导致断言失败并触发 Kernel BUG。\n\n## 解决方案\n用户通过回溯移植主线内核补丁修复了该问题，主要涉及 `bcache_4.15_backport_writeback.patch` 等补丁集。修复后执行分离操作前，需先执行 `echo none > /sys/block/bcacheX/bcache/cache_module`。"
}
