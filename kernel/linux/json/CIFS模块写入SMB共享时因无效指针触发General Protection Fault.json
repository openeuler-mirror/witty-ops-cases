{
  "title": "CIFS模块写入SMB共享时因无效指针触发General Protection Fault",
  "kernel_version": "4.13.0-32-generic",
  "phenomenon": "- 向 SMB 共享写入文件时系统崩溃并重启。\n- 控制台日志显示 \"general protection fault: 0000 [#1]\"。\n- 故障前日志多次出现 \"CIFS VFS: ioctl error in smb2_get_dfs_refer rc=-5\" 错误。\n- 崩溃发生在 `kmem_cache_alloc` 函数调用链中。",
  "root_cause": "崩溃发生在 `kmem_cache_alloc+0x7b`，调用栈显示为 `cifs_small_buf_get -> mempool_alloc -> kmem_cache_alloc`。故障发生时，传入 `kmem_cache_alloc` 的第一个参数（RDI 寄存器，应为 `struct kmem_cache *` 指针）值为 `0x2a180`，这是一个无效的内核地址（正常应为规范地址如 `ffff...`）。这表明 CIFS 模块在内存池操作中传递了损坏或未正确初始化的指针，导致 General Protection Fault。",
  "solution": "暂无（Bug 状态为 NEW，报告中未提供修复方案或补丁）。",
  "category": "kernel",
  "source_file": "CIFS模块写入SMB共享时因无效指针触发General Protection Fault.txt",
  "content": "内核版本：4.13.0-32-generic\n\n## 问题现象\n- 向 SMB 共享写入文件时系统崩溃并重启。\n- 控制台日志显示 \"general protection fault: 0000 [#1]\"。\n- 故障前日志多次出现 \"CIFS VFS: ioctl error in smb2_get_dfs_refer rc=-5\" 错误。\n- 崩溃发生在 `kmem_cache_alloc` 函数调用链中。\n\n## 问题根因\n崩溃发生在 `kmem_cache_alloc+0x7b`，调用栈显示为 `cifs_small_buf_get -> mempool_alloc -> kmem_cache_alloc`。故障发生时，传入 `kmem_cache_alloc` 的第一个参数（RDI 寄存器，应为 `struct kmem_cache *` 指针）值为 `0x2a180`，这是一个无效的内核地址（正常应为规范地址如 `ffff...`）。这表明 CIFS 模块在内存池操作中传递了损坏或未正确初始化的指针，导致 General Protection Fault。\n\n## 解决方案\n暂无（Bug 状态为 NEW，报告中未提供修复方案或补丁）。"
}
