{
  "title": "ixgbe驱动接收中断处理中调用eth_type_trans触发kernel panic",
  "kernel_version": "Debian 4.7.8-1~bpo8+1",
  "phenomenon": "- 系统每隔数天发生崩溃，触发 Kernel Panic。\n- 控制台输出 \"kernel BUG at .../include/linux/skbuff.h:1910\" 及 \"invalid opcode: 0000 [#1]\"。\n- 堆栈信息显示崩溃发生在中断上下文中，调用链为 `ixgbe_clean_rx_irq` -> `eth_type_trans`。\n- 最终报错：\"Kernel panic - not syncing: Fatal exception in interrupt\"。",
  "root_cause": "在 `ixgbe_clean_rx_irq` 函数处理网络接收数据包时，调用 `eth_type_trans` 触发了 `include/linux/skbuff.h:1910` 处的 `BUG()` 宏断言失败。这通常意味着传递给该函数的 `sk_buff` 结构体状态异常（如数据长度不足或指针错误），导致内核检测到非法状态并主动触发 panic。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "ixgbe驱动接收中断处理中调用eth_type_trans触发kernel panic.txt",
  "content": "内核版本：Debian 4.7.8-1~bpo8+1\n\n## 问题现象\n- 系统每隔数天发生崩溃，触发 Kernel Panic。\n- 控制台输出 \"kernel BUG at .../include/linux/skbuff.h:1910\" 及 \"invalid opcode: 0000 [#1]\"。\n- 堆栈信息显示崩溃发生在中断上下文中，调用链为 `ixgbe_clean_rx_irq` -> `eth_type_trans`。\n- 最终报错：\"Kernel panic - not syncing: Fatal exception in interrupt\"。\n\n## 问题根因\n在 `ixgbe_clean_rx_irq` 函数处理网络接收数据包时，调用 `eth_type_trans` 触发了 `include/linux/skbuff.h:1910` 处的 `BUG()` 宏断言失败。这通常意味着传递给该函数的 `sk_buff` 结构体状态异常（如数据长度不足或指针错误），导致内核检测到非法状态并主动触发 panic。\n\n## 解决方案\n暂无"
}
