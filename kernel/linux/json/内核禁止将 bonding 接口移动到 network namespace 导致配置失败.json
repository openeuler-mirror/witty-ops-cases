{
  "title": "内核禁止将 bonding 接口移动到 network namespace 导致配置失败",
  "kernel_version": "> 3.10.0-229.20.1",
  "phenomenon": "- 在 network namespace 中配置 bonding 接口时报错：\"Cannot find device 'bond0'\"\n- 系统日志显示：\"Master 'bond0': Error: handshake with driver failed. Aborting\"\n- 尝试将宿主机已有的 bond0 接口移动到新建的 netns 中时操作失败",
  "root_cause": "内核行为变更，有意禁止非硬件接口类型（如 bond、bridge 等）在不同 network namespace 之间移动。该限制从某版本开始引入，导致用户无法像旧版本那样直接将 bond 接口移入 netns。",
  "solution": "修改配置方式，不再尝试从外部移动 bond 接口，改为直接在目标 network namespace 内部创建 bond 接口。\n示例命令：\n`ip netns exec vntp ip link add dev bond0 type bond mode active-backup`\n随后在 netns 内部将物理网卡加入该 bond 即可。",
  "category": "kernel",
  "source_file": "内核禁止将 bonding 接口移动到 network namespace 导致配置失败.txt",
  "content": "内核版本：> 3.10.0-229.20.1\n\n## 问题现象\n- 在 network namespace 中配置 bonding 接口时报错：\"Cannot find device 'bond0'\"\n- 系统日志显示：\"Master 'bond0': Error: handshake with driver failed. Aborting\"\n- 尝试将宿主机已有的 bond0 接口移动到新建的 netns 中时操作失败\n\n## 问题根因\n内核行为变更，有意禁止非硬件接口类型（如 bond、bridge 等）在不同 network namespace 之间移动。该限制从某版本开始引入，导致用户无法像旧版本那样直接将 bond 接口移入 netns。\n\n## 解决方案\n修改配置方式，不再尝试从外部移动 bond 接口，改为直接在目标 network namespace 内部创建 bond 接口。\n示例命令：\n`ip netns exec vntp ip link add dev bond0 type bond mode active-backup`\n随后在 netns 内部将物理网卡加入该 bond 即可。"
}
