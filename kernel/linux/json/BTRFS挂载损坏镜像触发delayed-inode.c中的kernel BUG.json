{
  "title": "BTRFS挂载损坏镜像触发delayed-inode.c中的kernel BUG",
  "kernel_version": "5.0.0",
  "phenomenon": "- 挂载特制的模糊测试（fuzzed）btrfs 镜像并执行 unlink 操作时，触发 kernel BUG，系统打印 \"invalid opcode: 0000 [#1]\"。\n- 日志显示 \"kernel BUG at fs/btrfs/delayed-inode.c:1538!\"。\n- Call Trace 指向 `btrfs_delete_delayed_dir_index` -> `__btrfs_unlink_inode` -> `btrfs_unlink`。\n- 内核检测到元数据损坏：`BTRFS critical (device sdb): corrupt leaf...`。\n- 后续触发事务中止，文件系统强制变为只读模式。",
  "root_cause": "挂载了包含损坏元数据的特制 btrfs 镜像。内核在处理损坏的元数据时，`btrfs_delete_delayed_dir_index` 函数尝试将 delayed dir index item 添加到删除树时遇到错误（errno: -17），触发了 `fs/btrfs/delayed-inode.c:1538` 处的 `BUG()`。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "BTRFS挂载损坏镜像触发delayed-inode.c中的kernel BUG.txt",
  "content": "内核版本：5.0.0\n\n## 问题现象\n- 挂载特制的模糊测试（fuzzed）btrfs 镜像并执行 unlink 操作时，触发 kernel BUG，系统打印 \"invalid opcode: 0000 [#1]\"。\n- 日志显示 \"kernel BUG at fs/btrfs/delayed-inode.c:1538!\"。\n- Call Trace 指向 `btrfs_delete_delayed_dir_index` -> `__btrfs_unlink_inode` -> `btrfs_unlink`。\n- 内核检测到元数据损坏：`BTRFS critical (device sdb): corrupt leaf...`。\n- 后续触发事务中止，文件系统强制变为只读模式。\n\n## 问题根因\n挂载了包含损坏元数据的特制 btrfs 镜像。内核在处理损坏的元数据时，`btrfs_delete_delayed_dir_index` 函数尝试将 delayed dir index item 添加到删除树时遇到错误（errno: -17），触发了 `fs/btrfs/delayed-inode.c:1538` 处的 `BUG()`。\n\n## 解决方案\n暂无"
}
