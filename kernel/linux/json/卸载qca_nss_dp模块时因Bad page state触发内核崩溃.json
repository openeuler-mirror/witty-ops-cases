{
  "title": "卸载qca_nss_dp模块时因Bad page state触发内核崩溃",
  "kernel_version": "5.4.164",
  "phenomenon": "- 执行 `rmmod qca_nss_dp` 卸载 QCA NSS 数据通路模块时触发内核 BUG\n- 内核日志报错 \"BUG: Bad page state in process rmmod\"，指出页面标志位异常（flags: 0x200(slab)）\n- 调用栈显示异常路径为 `syn_dp_cfg_tx_cleanup_rings` -> `dma_free_attrs` -> `__free_pages_ok`",
  "root_cause": "在卸载 qca_nss_dp 驱动过程中，`syn_dp_cfg_tx_cleanup_rings` 函数调用 `dma_free_attrs` 释放内存时，内核检测到页面状态异常：被释放的页面仍带有 slab 标志（PAGE_FLAGS_CHECK_AT_FREE），这通常意味着内存被错误释放或存在双重释放，导致页面状态不一致。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "卸载qca_nss_dp模块时因Bad page state触发内核崩溃.txt",
  "content": "内核版本：5.4.164\n\n## 问题现象\n- 执行 `rmmod qca_nss_dp` 卸载 QCA NSS 数据通路模块时触发内核 BUG\n- 内核日志报错 \"BUG: Bad page state in process rmmod\"，指出页面标志位异常（flags: 0x200(slab)）\n- 调用栈显示异常路径为 `syn_dp_cfg_tx_cleanup_rings` -> `dma_free_attrs` -> `__free_pages_ok`\n\n## 问题根因\n在卸载 qca_nss_dp 驱动过程中，`syn_dp_cfg_tx_cleanup_rings` 函数调用 `dma_free_attrs` 释放内存时，内核检测到页面状态异常：被释放的页面仍带有 slab 标志（PAGE_FLAGS_CHECK_AT_FREE），这通常意味着内存被错误释放或存在双重释放，导致页面状态不一致。\n\n## 解决方案\n暂无"
}
