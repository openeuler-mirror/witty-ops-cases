{
  "title": "Eaton 5E UPS 初始化报告超时导致 USB 反复断开重连",
  "kernel_version": "v5.1",
  "phenomenon": "Eaton 5E UPS 设备通过 USB 连接后反复自动断开并重新枚举。dmesg 日志中持续出现以下错误信息：\n- `hid-generic 0003:0463:FFFF.00XX: usb_submit_urb(ctrl) failed: -1`\n- `hid-generic 0003:0463:FFFF.00XX: timeout initializing reports`\n- `usb 1-14: USB disconnect, device number XX`",
  "root_cause": "该 UPS 设备在 HID 子系统尝试初始化报告时发生超时，导致 `usb_submit_urb(ctrl)` 调用失败，进而触发设备断开连接并重新枚举。",
  "solution": "用户提供了临时补丁，通过修改 `drivers/hid/hid-quirks.c` 文件，为该设备添加 `HID_QUIRK_NO_INIT_REPORTS` 标志以跳过报告初始化，从而规避断开连接问题。上游维护者建议将问题提交至 linux-usb 邮件列表进行进一步处理。",
  "category": "kernel",
  "source_file": "Eaton 5E UPS 初始化报告超时导致 USB 反复断开重连.txt",
  "content": "内核版本：v5.1\n\n## 问题现象\nEaton 5E UPS 设备通过 USB 连接后反复自动断开并重新枚举。dmesg 日志中持续出现以下错误信息：\n- `hid-generic 0003:0463:FFFF.00XX: usb_submit_urb(ctrl) failed: -1`\n- `hid-generic 0003:0463:FFFF.00XX: timeout initializing reports`\n- `usb 1-14: USB disconnect, device number XX`\n\n## 问题根因\n该 UPS 设备在 HID 子系统尝试初始化报告时发生超时，导致 `usb_submit_urb(ctrl)` 调用失败，进而触发设备断开连接并重新枚举。\n\n## 解决方案\n用户提供了临时补丁，通过修改 `drivers/hid/hid-quirks.c` 文件，为该设备添加 `HID_QUIRK_NO_INIT_REPORTS` 标志以跳过报告初始化，从而规避断开连接问题。上游维护者建议将问题提交至 linux-usb 邮件列表进行进一步处理。"
}
