{
  "title": "workqueue模块worker_enter_idle函数触发内核WARNING",
  "kernel_version": "3.2.28",
  "phenomenon": "- 系统启动时在 shaper 初始化阶段（在 ifb0 设备上创建 HTB 分层 shaper，包含 hash filter table）触发内核 WARNING。\n- 日志显示：`WARNING: at kernel/workqueue.c:1225 worker_enter_idle+0xc7/0x120()`。\n- Call Trace 指向：`worker_enter_idle` -> `worker_thread` -> `kthread`。\n- 该问题在每次启动时均可复现。",
  "root_cause": "未明确",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "workqueue模块worker_enter_idle函数触发内核WARNING.txt",
  "content": "内核版本：3.2.28\n\n## 问题现象\n- 系统启动时在 shaper 初始化阶段（在 ifb0 设备上创建 HTB 分层 shaper，包含 hash filter table）触发内核 WARNING。\n- 日志显示：`WARNING: at kernel/workqueue.c:1225 worker_enter_idle+0xc7/0x120()`。\n- Call Trace 指向：`worker_enter_idle` -> `worker_thread` -> `kthread`。\n- 该问题在每次启动时均可复现。\n\n## 问题根因\n未明确\n\n## 解决方案\n暂无"
}
