{
  "title": "Netfilter规则替换时引用计数未清除导致内核崩溃",
  "kernel_version": "4.18.20",
  "phenomenon": "- 使用 `iptables -R` 替换指向自定义链的规则后，该链的引用计数未减少，出现“幽灵引用”（链显示有引用但无规则指向）。\n- 多次执行替换操作会导致引用计数累加。\n- 尝试使用 `iptables-restore` 恢复规则时进程挂起，内核日志报错：`kernel BUG at .../net/netfilter/nf_tables_api.c:1364!`。\n- 触发 `invalid opcode: 0000 [#1]`，Call Trace 显示在 `nf_tables_chain_destroy.isra.48` 和 `nf_tables_commit` 中。",
  "root_cause": "`nf_tables` 模块在处理规则替换逻辑时存在缺陷，未正确释放被替换规则对目标链的引用计数。当后续操作尝试销毁该链时，内核检测到引用计数异常，在 `nf_tables_chain_destroy` 函数中触发 `BUG()` 宏导致系统崩溃。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "Netfilter规则替换时引用计数未清除导致内核崩溃.txt",
  "content": "内核版本：4.18.20\n\n## 问题现象\n- 使用 `iptables -R` 替换指向自定义链的规则后，该链的引用计数未减少，出现“幽灵引用”（链显示有引用但无规则指向）。\n- 多次执行替换操作会导致引用计数累加。\n- 尝试使用 `iptables-restore` 恢复规则时进程挂起，内核日志报错：`kernel BUG at .../net/netfilter/nf_tables_api.c:1364!`。\n- 触发 `invalid opcode: 0000 [#1]`，Call Trace 显示在 `nf_tables_chain_destroy.isra.48` 和 `nf_tables_commit` 中。\n\n## 问题根因\n`nf_tables` 模块在处理规则替换逻辑时存在缺陷，未正确释放被替换规则对目标链的引用计数。当后续操作尝试销毁该链时，内核检测到引用计数异常，在 `nf_tables_chain_destroy` 函数中触发 `BUG()` 宏导致系统崩溃。\n\n## 解决方案\n暂无"
}
