{
  "title": "unmap_page_range函数触发General Protection Fault导致内核崩溃",
  "kernel_version": "v5.2-rc6",
  "phenomenon": "- 内核触发 General Protection Fault (GPF)，错误码 0000。\n- 故障发生在 `unmap_page_range+0xe9/0x1ab0` (mm/memory.c:1261)。\n- 调用栈显示崩溃发生在进程退出清理内存阶段 (`exit_mmap` -> `unmap_vmas` -> `unmap_page_range`)。\n- KASAN 日志提示 GPF 可能由 NULL-ptr deref 引起，寄存器 RBX 值为 0。",
  "root_cause": "通过 Syzkaller 执行 `/dev/sg#` 设备的 `SCSI_IOCTL_SEND_COMMAND` 操作触发。故障发生在进程退出执行 `exit_mmap` 清理内存时，`unmap_page_range` 函数内部访问了无效地址（RBX 寄存器为 0），导致 General Protection Fault。具体代码逻辑错误尚待分析。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "unmap_page_range函数触发General Protection Fault导致内核崩溃.txt",
  "content": "内核版本：v5.2-rc6\n\n## 问题现象\n- 内核触发 General Protection Fault (GPF)，错误码 0000。\n- 故障发生在 `unmap_page_range+0xe9/0x1ab0` (mm/memory.c:1261)。\n- 调用栈显示崩溃发生在进程退出清理内存阶段 (`exit_mmap` -> `unmap_vmas` -> `unmap_page_range`)。\n- KASAN 日志提示 GPF 可能由 NULL-ptr deref 引起，寄存器 RBX 值为 0。\n\n## 问题根因\n通过 Syzkaller 执行 `/dev/sg#` 设备的 `SCSI_IOCTL_SEND_COMMAND` 操作触发。故障发生在进程退出执行 `exit_mmap` 清理内存时，`unmap_page_range` 函数内部访问了无效地址（RBX 寄存器为 0），导致 General Protection Fault。具体代码逻辑错误尚待分析。\n\n## 解决方案\n暂无"
}
