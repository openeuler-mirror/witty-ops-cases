{
  "title": "tcp_keepalive_timer 函数引发 soft lockup",
  "kernel_version": "3.0.93",
  "phenomenon": "- 内核日志报错 \"BUG: soft lockup - CPU#3 stuck for 22s!\"\n- 进程 neutron-server 在执行 tcp_keepalive_timer 时卡住。\n- Call Trace 显示路径为：_raw_spin_lock <- tcp_keepalive_timer <- call_timer_fn <- run_timer_softirq。\n- 单个 CPU 在 bh_lock_sock(sk) 处死锁，其他 CPU 空闲。",
  "root_cause": "调用 tcp_keepalive_timer 时，在 bh_lock_sock(sk) 获取锁的过程中发生死锁，导致 CPU 无法调度。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "tcp_keepalive_timer 函数引发 soft lockup.txt",
  "content": "内核版本：3.0.93\n\n## 问题现象\n- 内核日志报错 \"BUG: soft lockup - CPU#3 stuck for 22s!\"\n- 进程 neutron-server 在执行 tcp_keepalive_timer 时卡住。\n- Call Trace 显示路径为：_raw_spin_lock <- tcp_keepalive_timer <- call_timer_fn <- run_timer_softirq。\n- 单个 CPU 在 bh_lock_sock(sk) 处死锁，其他 CPU 空闲。\n\n## 问题根因\n调用 tcp_keepalive_timer 时，在 bh_lock_sock(sk) 获取锁的过程中发生死锁，导致 CPU 无法调度。\n\n## 解决方案\n暂无"
}
