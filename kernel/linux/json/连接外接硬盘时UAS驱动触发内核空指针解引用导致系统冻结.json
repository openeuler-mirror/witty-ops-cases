{
  "title": "连接外接硬盘时UAS驱动触发内核空指针解引用导致系统冻结",
  "kernel_version": "3.17.1-1-ARCH",
  "phenomenon": "- 连接外接硬盘后约30秒系统冻结无响应。\n- 内核日志显示 \"BUG: unable to handle kernel NULL pointer dereference at 00000000000000f0\"。\n- 崩溃发生在 `_raw_spin_lock_irqsave` 函数中。\n- 日志上下文显示 `uas_eh_abort_handler` 正在处理 SCSI 设备。\n- systemd-udevd 进程出现超时并尝试终止 worker。",
  "root_cause": "UAS（USB Attached SCSI）驱动在执行 `uas_eh_abort_handler` 异常处理流程时，尝试对一个空指针进行操作（访问偏移量为 0xf0 的成员），导致在调用 `_raw_spin_lock_irqsave` 加锁时触发内核空指针解引用。",
  "solution": "暂无。开发者建议将日志信息发送至 linux-usb@vger.kernel.org 邮件列表以获取进一步支持。",
  "category": "kernel",
  "source_file": "连接外接硬盘时UAS驱动触发内核空指针解引用导致系统冻结.txt",
  "content": "内核版本：3.17.1-1-ARCH\n\n## 问题现象\n- 连接外接硬盘后约30秒系统冻结无响应。\n- 内核日志显示 \"BUG: unable to handle kernel NULL pointer dereference at 00000000000000f0\"。\n- 崩溃发生在 `_raw_spin_lock_irqsave` 函数中。\n- 日志上下文显示 `uas_eh_abort_handler` 正在处理 SCSI 设备。\n- systemd-udevd 进程出现超时并尝试终止 worker。\n\n## 问题根因\nUAS（USB Attached SCSI）驱动在执行 `uas_eh_abort_handler` 异常处理流程时，尝试对一个空指针进行操作（访问偏移量为 0xf0 的成员），导致在调用 `_raw_spin_lock_irqsave` 加锁时触发内核空指针解引用。\n\n## 解决方案\n暂无。开发者建议将日志信息发送至 linux-usb@vger.kernel.org 邮件列表以获取进一步支持。"
}
