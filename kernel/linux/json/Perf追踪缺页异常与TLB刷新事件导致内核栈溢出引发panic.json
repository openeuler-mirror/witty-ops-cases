{
  "title": "Perf追踪缺页异常与TLB刷新事件导致内核栈溢出引发panic",
  "kernel_version": "5.4及以上版本",
  "phenomenon": "- 执行命令 `sudo perf record -eexceptions:page_fault_user,exceptions:page_fault_kernel,tlb:tlb_flush sleep 1` 时触发 kernel panic。\n- 日志显示 \"BUG: stack guard page was hit\" 及 \"kernel stack overflow (double-fault)\"。\n- 故障现场调用栈指向 `perf_trace_x86_exceptions` 函数，涉及 `do_page_fault` 等处理流程。",
  "root_cause": "由 commit 763802b53a427ed3cbd419dbba255c414fdd9e7c (`x86/mm: split vmalloc_sync_all()`) 引入的回归。该修改导致在追踪缺页异常和 TLB 刷新事件时，内核栈空间耗尽并触发 double-fault。",
  "solution": "暂无官方修复补丁（状态为 NEW）。建议避免同时追踪 `exceptions:page_fault_user`、`exceptions:page_fault_kernel` 和 `tlb:tlb_flush` 事件组合以规避问题。",
  "category": "kernel",
  "source_file": "Perf追踪缺页异常与TLB刷新事件导致内核栈溢出引发panic.txt",
  "content": "内核版本：5.4及以上版本\n\n## 问题现象\n- 执行命令 `sudo perf record -eexceptions:page_fault_user,exceptions:page_fault_kernel,tlb:tlb_flush sleep 1` 时触发 kernel panic。\n- 日志显示 \"BUG: stack guard page was hit\" 及 \"kernel stack overflow (double-fault)\"。\n- 故障现场调用栈指向 `perf_trace_x86_exceptions` 函数，涉及 `do_page_fault` 等处理流程。\n\n## 问题根因\n由 commit 763802b53a427ed3cbd419dbba255c414fdd9e7c (`x86/mm: split vmalloc_sync_all()`) 引入的回归。该修改导致在追踪缺页异常和 TLB 刷新事件时，内核栈空间耗尽并触发 double-fault。\n\n## 解决方案\n暂无官方修复补丁（状态为 NEW）。建议避免同时追踪 `exceptions:page_fault_user`、`exceptions:page_fault_kernel` 和 `tlb:tlb_flush` 事件组合以规避问题。"
}
