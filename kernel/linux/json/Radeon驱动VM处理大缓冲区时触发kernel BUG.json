{
  "title": "Radeon驱动VM处理大缓冲区时触发kernel BUG",
  "kernel_version": "3.13.5",
  "phenomenon": "- 系统日志报错：`kernel BUG at drivers/gpu/drm/radeon/radeon_sa.c:322!`\n- 触发 invalid opcode (0000 [#1])，导致进程崩溃。\n- 调用栈显示：`radeon_sa_bo_new+0x49d/0x4c0 [radeon]` -> `radeon_ib_get` -> `radeon_vm_bo_update` -> `radeon_cs_ioctl`。\n- 复现场景：使用 Firefox 浏览 Google 街景时触发。",
  "root_cause": "Radeon 驱动的 VM 实现存在已知缺陷。应用程序请求的缓冲区大小超过了 VM 代码的处理能力，但该大小未触发正常的错误处理路径，导致在 `radeon_sa_bo_new` 函数中触发 `BUG()`。",
  "solution": "该问题在 `drm-next-3.15-wip` 分支中已修复，建议升级至后续内核版本。",
  "category": "kernel",
  "source_file": "Radeon驱动VM处理大缓冲区时触发kernel BUG.txt",
  "content": "内核版本：3.13.5\n\n## 问题现象\n- 系统日志报错：`kernel BUG at drivers/gpu/drm/radeon/radeon_sa.c:322!`\n- 触发 invalid opcode (0000 [#1])，导致进程崩溃。\n- 调用栈显示：`radeon_sa_bo_new+0x49d/0x4c0 [radeon]` -> `radeon_ib_get` -> `radeon_vm_bo_update` -> `radeon_cs_ioctl`。\n- 复现场景：使用 Firefox 浏览 Google 街景时触发。\n\n## 问题根因\nRadeon 驱动的 VM 实现存在已知缺陷。应用程序请求的缓冲区大小超过了 VM 代码的处理能力，但该大小未触发正常的错误处理路径，导致在 `radeon_sa_bo_new` 函数中触发 `BUG()`。\n\n## 解决方案\n该问题在 `drm-next-3.15-wip` 分支中已修复，建议升级至后续内核版本。"
}
