{
  "title": "移除HUGETLB_PAGE_DTOR导致内存压缩时触发kernel BUG",
  "kernel_version": "v6.6-rc5",
  "phenomenon": "- 运行 XFS fstests 测试（如 xfs/066）时系统触发 kernel BUG。\n- 日志显示：`kernel BUG at include/linux/page-flags.h:314!`，伴随 `invalid opcode: 0000 [#1]` 错误。\n- 触发断言：`VM_BUG_ON_PAGE`，页面类型显示为 `buddy`。\n- 调用栈指向：`folio_flags` -> `PageHuge` -> `isolate_migratepages_block` -> `compact_zone`。",
  "root_cause": "开发者分析认为该问题由 commit 9c5ccf2db04b (\"mm: remove HUGETLB_PAGE_DTOR\") 引入。在内存压缩流程中，`isolate_migratepages_block` 函数调用 `PageHuge` 检查页面属性时，因页面标志位判断逻辑异常，触发了 `include/linux/page-flags.h:314` 处的 `VM_BUG_ON_PAGE` 断言失败。",
  "solution": "暂无修复补丁，开发者建议在相关邮件列表线程中继续讨论。",
  "category": "kernel",
  "source_file": "移除HUGETLB_PAGE_DTOR导致内存压缩时触发kernel BUG.txt",
  "content": "内核版本：v6.6-rc5\n\n## 问题现象\n- 运行 XFS fstests 测试（如 xfs/066）时系统触发 kernel BUG。\n- 日志显示：`kernel BUG at include/linux/page-flags.h:314!`，伴随 `invalid opcode: 0000 [#1]` 错误。\n- 触发断言：`VM_BUG_ON_PAGE`，页面类型显示为 `buddy`。\n- 调用栈指向：`folio_flags` -> `PageHuge` -> `isolate_migratepages_block` -> `compact_zone`。\n\n## 问题根因\n开发者分析认为该问题由 commit 9c5ccf2db04b (\"mm: remove HUGETLB_PAGE_DTOR\") 引入。在内存压缩流程中，`isolate_migratepages_block` 函数调用 `PageHuge` 检查页面属性时，因页面标志位判断逻辑异常，触发了 `include/linux/page-flags.h:314` 处的 `VM_BUG_ON_PAGE` 断言失败。\n\n## 解决方案\n暂无修复补丁，开发者建议在相关邮件列表线程中继续讨论。"
}
