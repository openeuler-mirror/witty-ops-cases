{
  "title": "vgacon_scroll 函数触发 use-after-free 导致内核内存错误",
  "kernel_version": "5.7.0+",
  "phenomenon": "- 内核触发 KASAN 报警：BUG: KASAN: use-after-free in vgacon_scroll\n- 系统日志显示在 `vgacon_scroll` 函数中发生了非法内存读取\n- 故障进程为 `(agetty)`，调用栈涉及 `con_scroll`、`do_con_write` 等控制台写入路径",
  "root_cause": "`vgacon_scroll` 函数在执行控制台滚动操作时，通过 `include/linux/vt_buffer.h:49` 处的 `memcpy` 尝试读取大小为 2101248 字节的内存区域，但该内存地址已被释放，导致发生 use-after-free 错误。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "vgacon_scroll 函数触发 use-after-free 导致内核内存错误.txt",
  "content": "内核版本：5.7.0+\n\n## 问题现象\n- 内核触发 KASAN 报警：BUG: KASAN: use-after-free in vgacon_scroll\n- 系统日志显示在 `vgacon_scroll` 函数中发生了非法内存读取\n- 故障进程为 `(agetty)`，调用栈涉及 `con_scroll`、`do_con_write` 等控制台写入路径\n\n## 问题根因\n`vgacon_scroll` 函数在执行控制台滚动操作时，通过 `include/linux/vt_buffer.h:49` 处的 `memcpy` 尝试读取大小为 2101248 字节的内存区域，但该内存地址已被释放，导致发生 use-after-free 错误。\n\n## 解决方案\n暂无"
}
