{
  "title": "开启THP时futex调用触发VM_BUG_ON导致内核崩溃",
  "kernel_version": "2.6.32",
  "phenomenon": "- 系统偶发 kernel BUG，日志显示 \"kernel BUG at mm/filemap.c:588!\" 及 \"invalid opcode: 0000 [#1]\"。\n- 触发 BUG 时的 Call Trace 为：`unlock_page` <- `get_futex_key` <- `futex_wait_setup` <- `futex_wait` <- `do_futex` <- `sys_futex`。",
  "root_cause": "在 2.6.32 版本内核中，透明大页（THP）功能在 `get_futex_key` 函数中的处理逻辑存在缺陷。当涉及 THP 页面时，页面锁定状态管理不一致，导致在 `unlock_page` 函数中触发 `VM_BUG_ON(PageLocked(page))` 检查失败。",
  "solution": "规避措施：关闭透明大页功能，执行命令 `echo never > /sys/kernel/mm/redhat_transparent_hugepage/enabled`。\n根本措施：升级内核版本，早期 THP 实现中的相关缺陷已在后续版本中修复。",
  "category": "kernel",
  "source_file": "开启THP时futex调用触发VM_BUG_ON导致内核崩溃.txt",
  "content": "内核版本：2.6.32\n\n## 问题现象\n- 系统偶发 kernel BUG，日志显示 \"kernel BUG at mm/filemap.c:588!\" 及 \"invalid opcode: 0000 [#1]\"。\n- 触发 BUG 时的 Call Trace 为：`unlock_page` <- `get_futex_key` <- `futex_wait_setup` <- `futex_wait` <- `do_futex` <- `sys_futex`。\n\n## 问题根因\n在 2.6.32 版本内核中，透明大页（THP）功能在 `get_futex_key` 函数中的处理逻辑存在缺陷。当涉及 THP 页面时，页面锁定状态管理不一致，导致在 `unlock_page` 函数中触发 `VM_BUG_ON(PageLocked(page))` 检查失败。\n\n## 解决方案\n规避措施：关闭透明大页功能，执行命令 `echo never > /sys/kernel/mm/redhat_transparent_hugepage/enabled`。\n根本措施：升级内核版本，早期 THP 实现中的相关缺陷已在后续版本中修复。"
}
