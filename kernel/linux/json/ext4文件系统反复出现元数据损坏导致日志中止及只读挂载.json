{
  "title": "ext4文件系统反复出现元数据损坏导致日志中止及只读挂载",
  "kernel_version": "4.10.0（问题复现于 4.9.9）",
  "phenomenon": "- 内核日志报告 EXT4-fs error，具体包括：`deleted inode referenced`、`Unrecognised inode hash code`、`Corrupt directory`。\n- 触发 `Aborting journal on device`，文件系统自动重挂载为只读模式。\n- 运行 `tar` 或 `find` 命令时触发大量文件系统错误。\n- 用户态进程（如 exim4）发生 segfault。",
  "root_cause": "未明确。用户推测问题为内核 4.9.0 至 4.9.9 版本间的回归。故障环境涉及复杂的存储栈结构：ext4 -> LVM -> dm-cache -> LVM -> md (RAID-6)。",
  "solution": "暂无官方修复。用户尝试降级内核至 4.9.0 以验证问题是否消失。",
  "category": "kernel",
  "source_file": "ext4文件系统反复出现元数据损坏导致日志中止及只读挂载.txt",
  "content": "内核版本：4.10.0（问题复现于 4.9.9）\n\n## 问题现象\n- 内核日志报告 EXT4-fs error，具体包括：`deleted inode referenced`、`Unrecognised inode hash code`、`Corrupt directory`。\n- 触发 `Aborting journal on device`，文件系统自动重挂载为只读模式。\n- 运行 `tar` 或 `find` 命令时触发大量文件系统错误。\n- 用户态进程（如 exim4）发生 segfault。\n\n## 问题根因\n未明确。用户推测问题为内核 4.9.0 至 4.9.9 版本间的回归。故障环境涉及复杂的存储栈结构：ext4 -> LVM -> dm-cache -> LVM -> md (RAID-6)。\n\n## 解决方案\n暂无官方修复。用户尝试降级内核至 4.9.0 以验证问题是否消失。"
}
