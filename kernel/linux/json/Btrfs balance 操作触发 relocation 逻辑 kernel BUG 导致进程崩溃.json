{
  "title": "Btrfs balance 操作触发 relocation 逻辑 kernel BUG 导致进程崩溃",
  "kernel_version": "4.4.0-22-generic（评论显示 5.11.0-gentoo、6.1.31_1 版本仍受影响）",
  "phenomenon": "- 执行 `btrfs balance` 命令时，用户态进程收到 Segmentation fault 信号退出。\n- 内核日志打印 `kernel BUG at fs/btrfs/relocation.c` 及 `invalid opcode: 0000 [#1]` 错误。\n- 不同内核版本崩溃位置不同：\n  - 4.4 内核：`do_relocation+0x515/0x520` (relocation.c:2683)\n  - 5.11 内核：`create_reloc_inode+0x109/0x240` (relocation.c:3442)\n  - 6.1 内核：`btrfs_reloc_cow_block+0x29f/0x2b0` (relocation.c:4391)\n- 故障可能导致文件系统无法挂载或系统挂起。",
  "root_cause": "Btrfs 模块在执行数据重定位（relocation）过程中触发了内核代码中的 `BUG()` 宏断言失败。该问题跨越多个内核版本长期存在，涉及 `do_relocation`、`create_reloc_inode` 或 `btrfs_reloc_cow_block` 等函数，表明 relocation 流程中存在未预期的异常状态或边界条件处理缺陷。",
  "solution": "暂无（Bug 状态为 NEW，尚未修复）。",
  "category": "kernel",
  "source_file": "Btrfs balance 操作触发 relocation 逻辑 kernel BUG 导致进程崩溃.txt",
  "content": "内核版本：4.4.0-22-generic（评论显示 5.11.0-gentoo、6.1.31_1 版本仍受影响）\n\n## 问题现象\n- 执行 `btrfs balance` 命令时，用户态进程收到 Segmentation fault 信号退出。\n- 内核日志打印 `kernel BUG at fs/btrfs/relocation.c` 及 `invalid opcode: 0000 [#1]` 错误。\n- 不同内核版本崩溃位置不同：\n  - 4.4 内核：`do_relocation+0x515/0x520` (relocation.c:2683)\n  - 5.11 内核：`create_reloc_inode+0x109/0x240` (relocation.c:3442)\n  - 6.1 内核：`btrfs_reloc_cow_block+0x29f/0x2b0` (relocation.c:4391)\n- 故障可能导致文件系统无法挂载或系统挂起。\n\n## 问题根因\nBtrfs 模块在执行数据重定位（relocation）过程中触发了内核代码中的 `BUG()` 宏断言失败。该问题跨越多个内核版本长期存在，涉及 `do_relocation`、`create_reloc_inode` 或 `btrfs_reloc_cow_block` 等函数，表明 relocation 流程中存在未预期的异常状态或边界条件处理缺陷。\n\n## 解决方案\n暂无（Bug 状态为 NEW，尚未修复）。"
}
