{
  "title": "Intel I350网卡接收数据时触发General Protection Fault",
  "kernel_version": "4.6.4, 4.4.0",
  "phenomenon": "- 系统作为路由器/防火墙运行时，出现偶发性内核崩溃（约每月一次）。\n- 日志记录显示 `general protection fault: 0000 [#1]`。\n- 崩溃发生在 `memcpy_erms` 函数中，调用栈显示路径为 `igb_poll` -> `napi_gro_receive` -> `skb_copy_ubufs` -> `memcpy_erms`。\n- 寄存器 RSI 显示为无效地址（如 `032927e6cb14cb7a`）。\n- 另一实例触发 `kernel BUG at skbuff.h:1825`，位于 `eth_type_trans` 函数。",
  "root_cause": "在 `igb` 驱动的接收路径中，`skb_copy_ubufs` 调用 `memcpy` 时使用了无效的源地址指针（RSI 寄存器值异常），导致 General Protection Fault。具体导致指针损坏的深层原因未明确。",
  "solution": "暂无（Bug 状态为 NEW，报告中未提供修复补丁）。",
  "category": "kernel",
  "source_file": "Intel I350网卡接收数据时触发General Protection Fault.txt",
  "content": "内核版本：4.6.4, 4.4.0\n\n## 问题现象\n- 系统作为路由器/防火墙运行时，出现偶发性内核崩溃（约每月一次）。\n- 日志记录显示 `general protection fault: 0000 [#1]`。\n- 崩溃发生在 `memcpy_erms` 函数中，调用栈显示路径为 `igb_poll` -> `napi_gro_receive` -> `skb_copy_ubufs` -> `memcpy_erms`。\n- 寄存器 RSI 显示为无效地址（如 `032927e6cb14cb7a`）。\n- 另一实例触发 `kernel BUG at skbuff.h:1825`，位于 `eth_type_trans` 函数。\n\n## 问题根因\n在 `igb` 驱动的接收路径中，`skb_copy_ubufs` 调用 `memcpy` 时使用了无效的源地址指针（RSI 寄存器值异常），导致 General Protection Fault。具体导致指针损坏的深层原因未明确。\n\n## 解决方案\n暂无（Bug 状态为 NEW，报告中未提供修复补丁）。"
}
