{
  "title": "dmabuffs_dname函数访问已释放内存触发KASAN use-after-free错误",
  "kernel_version": "4.19.81",
  "phenomenon": "- KASAN 检测到 use-after-free 错误：`BUG: KASAN: use-after-free in dmabuffs_dname+0x54/0xf0`\n- 进程 `lsof` (PID 32197) 在读取路径时触发异常，Call trace 显示调用链为 `dmabuffs_dname` -> `d_path` -> `proc_pid_readlink` -> `vfs_readlink`。\n- 系统日志记录了内存分配来源为 `ion_alloc`，释放来源为 `dma_buf_release`。",
  "root_cause": "DMA-BUF 对象生命周期管理存在竞争或引用计数问题。内存经 `ion_alloc` 分配后，由 `dma_buf_release` 释放，但 `lsof` 进程随后在调用 `dmabuffs_dname` 构建 dentry 名称时访问了该已释放内存区域，导致 use-after-free。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "dmabuffs_dname函数访问已释放内存触发KASAN use-after-free错误.txt",
  "content": "内核版本：4.19.81\n\n## 问题现象\n- KASAN 检测到 use-after-free 错误：`BUG: KASAN: use-after-free in dmabuffs_dname+0x54/0xf0`\n- 进程 `lsof` (PID 32197) 在读取路径时触发异常，Call trace 显示调用链为 `dmabuffs_dname` -> `d_path` -> `proc_pid_readlink` -> `vfs_readlink`。\n- 系统日志记录了内存分配来源为 `ion_alloc`，释放来源为 `dma_buf_release`。\n\n## 问题根因\nDMA-BUF 对象生命周期管理存在竞争或引用计数问题。内存经 `ion_alloc` 分配后，由 `dma_buf_release` 释放，但 `lsof` 进程随后在调用 `dmabuffs_dname` 构建 dentry 名称时访问了该已释放内存区域，导致 use-after-free。\n\n## 解决方案\n暂无"
}
