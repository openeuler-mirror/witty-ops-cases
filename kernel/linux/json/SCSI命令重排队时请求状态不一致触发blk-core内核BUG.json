{
  "title": "SCSI命令重排队时请求状态不一致触发blk-core内核BUG",
  "kernel_version": "3.10.0-123.el7.x86_64",
  "phenomenon": "- 系统日志显示 I/O 错误，qla2xxx 驱动频繁发出 Abort command 和 Device Reset 指令。\n- 触发 kernel BUG，日志打印 \"kernel BUG at block/blk-core.c:1217!\"。\n- 系统发生 panic，提示 \"invalid opcode: 0000 [#1]\"。\n- 调用栈显示路径为：`blk_requeue_request` <- `__scsi_queue_insert` <- `scsi_softirq_done`。",
  "root_cause": "在 `blk_requeue_request` 函数中，代码执行 `BUG_ON(blk_queued_rq(rq))` 检查。该检查期望请求（request）处于已排队状态（即 `cmd_flags` 包含 `REQ_QUEUED` 标志且位于 `tag_busy_list` 中）。实际故障发生时，请求被添加到了 `queue_head` 而非 `tag_busy_list`，且 `cmd_flags` 未包含 `REQ_QUEUED` 标志，导致断言失败触发 BUG。这表明在 SCSI 错误处理或命令重排队路径中存在请求状态不一致的问题。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "SCSI命令重排队时请求状态不一致触发blk-core内核BUG.txt",
  "content": "内核版本：3.10.0-123.el7.x86_64\n\n## 问题现象\n- 系统日志显示 I/O 错误，qla2xxx 驱动频繁发出 Abort command 和 Device Reset 指令。\n- 触发 kernel BUG，日志打印 \"kernel BUG at block/blk-core.c:1217!\"。\n- 系统发生 panic，提示 \"invalid opcode: 0000 [#1]\"。\n- 调用栈显示路径为：`blk_requeue_request` <- `__scsi_queue_insert` <- `scsi_softirq_done`。\n\n## 问题根因\n在 `blk_requeue_request` 函数中，代码执行 `BUG_ON(blk_queued_rq(rq))` 检查。该检查期望请求（request）处于已排队状态（即 `cmd_flags` 包含 `REQ_QUEUED` 标志且位于 `tag_busy_list` 中）。实际故障发生时，请求被添加到了 `queue_head` 而非 `tag_busy_list`，且 `cmd_flags` 未包含 `REQ_QUEUED` 标志，导致断言失败触发 BUG。这表明在 SCSI 错误处理或命令重排队路径中存在请求状态不一致的问题。\n\n## 解决方案\n暂无"
}
