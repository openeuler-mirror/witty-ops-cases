{
  "title": "Btrfs在设备由只读转为读写后无法重新挂载为读写模式",
  "kernel_version": "5.7.0-1.fc33.x86_64",
  "phenomenon": "- 当块设备初始状态为只读（RO）并被挂载后，若通过 `blockdev --setrw` 将设备状态改为读写（RW），执行 `mount -o remount,rw` 重新挂载文件系统失败。\n- 终端报错：`mount: ...: cannot remount ... read-write, is write-protected.`\n- 相同操作流程下，XFS 文件系统可正常重新挂载为读写模式。",
  "root_cause": "Btrfs 文件系统在处理 remount 请求时，未能正确检测到底层块设备状态已由只读变为读写，错误地判定设备仍处于写保护状态从而拒绝挂载请求。",
  "solution": "暂无修复补丁。临时规避方法：先卸载文件系统，再重新以读写模式挂载。",
  "category": "kernel",
  "source_file": "Btrfs在设备由只读转为读写后无法重新挂载为读写模式.txt",
  "content": "内核版本：5.7.0-1.fc33.x86_64\n\n## 问题现象\n- 当块设备初始状态为只读（RO）并被挂载后，若通过 `blockdev --setrw` 将设备状态改为读写（RW），执行 `mount -o remount,rw` 重新挂载文件系统失败。\n- 终端报错：`mount: ...: cannot remount ... read-write, is write-protected.`\n- 相同操作流程下，XFS 文件系统可正常重新挂载为读写模式。\n\n## 问题根因\nBtrfs 文件系统在处理 remount 请求时，未能正确检测到底层块设备状态已由只读变为读写，错误地判定设备仍处于写保护状态从而拒绝挂载请求。\n\n## 解决方案\n暂无修复补丁。临时规避方法：先卸载文件系统，再重新以读写模式挂载。"
}
