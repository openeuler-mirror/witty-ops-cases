{
  "title": "percpu变量__preempt_count值异常导致do_exit触发panic",
  "kernel_version": "4.19.90",
  "phenomenon": "- 系统触发 kernel panic，错误信息为 \"Aiee, killing interrupt handler!\"。\n- 故障堆栈显示进程（docker，PID: 178951）在执行 `exit_group` 系统调用时，于 `do_exit` 函数中触发崩溃。\n- 故障现场检查发现，percpu 变量 `__preempt_count` 的值异常变为 `0x7fffffe00`。",
  "root_cause": "用户推测某处代码额外调用了 `local_bh_enable()` 函数导致计数异常。据分析，在 preemptible 模式下 `__preempt_count` 正常值应为 `0x80000000`，额外调用一次 `local_bh_enable()` 导致该值变为 `0x7fffffe00`。该异常值导致 `do_exit` 中的 `in_interrupt()` 检查返回真（误判当前处于中断上下文），从而触发 panic。",
  "solution": "暂无（Bug 状态为 NEW，尚未确认修复方案）。",
  "category": "kernel",
  "source_file": "percpu变量__preempt_count值异常导致do_exit触发panic.txt",
  "content": "内核版本：4.19.90\n\n## 问题现象\n- 系统触发 kernel panic，错误信息为 \"Aiee, killing interrupt handler!\"。\n- 故障堆栈显示进程（docker，PID: 178951）在执行 `exit_group` 系统调用时，于 `do_exit` 函数中触发崩溃。\n- 故障现场检查发现，percpu 变量 `__preempt_count` 的值异常变为 `0x7fffffe00`。\n\n## 问题根因\n用户推测某处代码额外调用了 `local_bh_enable()` 函数导致计数异常。据分析，在 preemptible 模式下 `__preempt_count` 正常值应为 `0x80000000`，额外调用一次 `local_bh_enable()` 导致该值变为 `0x7fffffe00`。该异常值导致 `do_exit` 中的 `in_interrupt()` 检查返回真（误判当前处于中断上下文），从而触发 panic。\n\n## 解决方案\n暂无（Bug 状态为 NEW，尚未确认修复方案）。"
}
