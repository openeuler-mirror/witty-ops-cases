{
  "title": "AMD Renoir平台S3唤醒时中断过早启用触发内核警告",
  "kernel_version": "6.1.21",
  "phenomenon": "- 系统从S3挂起状态恢复时触发内核警告：\"Interrupts enabled before system core resume\"\n- Call Trace指向 `syscore_resume+0x173/0x180`\n- 随后伴随出现 \"BUG: using smp_processor_id() in preemptible code\" 和 \"BUG: using __this_cpu_read() in preemptible code\" 错误",
  "root_cause": "在 AMD Renoir APU 平台上，系统恢复过程中中断在 `syscore_resume` 执行前被意外启用。报告者初步分析认为可能是固件问题，因为在底层恢复代码与 `syscore_resume` 之间未观察到显式的中断相关代码执行。该问题复现概率极低（数百次S3循环中出现一两次）。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "AMD Renoir平台S3唤醒时中断过早启用触发内核警告.txt",
  "content": "内核版本：6.1.21\n\n## 问题现象\n- 系统从S3挂起状态恢复时触发内核警告：\"Interrupts enabled before system core resume\"\n- Call Trace指向 `syscore_resume+0x173/0x180`\n- 随后伴随出现 \"BUG: using smp_processor_id() in preemptible code\" 和 \"BUG: using __this_cpu_read() in preemptible code\" 错误\n\n## 问题根因\n在 AMD Renoir APU 平台上，系统恢复过程中中断在 `syscore_resume` 执行前被意外启用。报告者初步分析认为可能是固件问题，因为在底层恢复代码与 `syscore_resume` 之间未观察到显式的中断相关代码执行。该问题复现概率极低（数百次S3循环中出现一两次）。\n\n## 解决方案\n暂无"
}
