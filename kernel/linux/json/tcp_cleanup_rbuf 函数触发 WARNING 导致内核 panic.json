{
  "title": "tcp_cleanup_rbuf 函数触发 WARNING 导致内核 panic",
  "kernel_version": "5.7.0+",
  "phenomenon": "- 内核触发 WARNING：`WARNING: CPU: 1 PID: 955 at net/ipv4/tcp.c:1537 tcp_cleanup_rbuf+0x99/0x330`\n- 日志打印错误信息：`cleanup rbuf bug: copied 80 seq 1 rcvnxt 80`\n- 由于系统设置了 `panic_on_warn`，触发 Kernel Panic 导致系统重启\n- 调用栈显示问题发生在 `tcp_recvmsg` 处理路径中",
  "root_cause": "在 TCP 接收消息处理流程中，`tcp_cleanup_rbuf` 函数内部触发了 `WARN()` 断言失败。日志信息 `cleanup rbuf bug: copied 80 seq 1 rcvnxt 80` 指示 TCP 接收缓冲区清理逻辑中存在状态不一致（涉及 `copied`、`seq`、`rcvnxt` 变量）。",
  "solution": "暂无（Bug 状态为 NEW，尚未合并修复补丁）",
  "category": "kernel",
  "source_file": "tcp_cleanup_rbuf 函数触发 WARNING 导致内核 panic.txt",
  "content": "内核版本：5.7.0+\n\n## 问题现象\n- 内核触发 WARNING：`WARNING: CPU: 1 PID: 955 at net/ipv4/tcp.c:1537 tcp_cleanup_rbuf+0x99/0x330`\n- 日志打印错误信息：`cleanup rbuf bug: copied 80 seq 1 rcvnxt 80`\n- 由于系统设置了 `panic_on_warn`，触发 Kernel Panic 导致系统重启\n- 调用栈显示问题发生在 `tcp_recvmsg` 处理路径中\n\n## 问题根因\n在 TCP 接收消息处理流程中，`tcp_cleanup_rbuf` 函数内部触发了 `WARN()` 断言失败。日志信息 `cleanup rbuf bug: copied 80 seq 1 rcvnxt 80` 指示 TCP 接收缓冲区清理逻辑中存在状态不一致（涉及 `copied`、`seq`、`rcvnxt` 变量）。\n\n## 解决方案\n暂无（Bug 状态为 NEW，尚未合并修复补丁）"
}
