{
  "title": "测试 cpu_idle trace event 时在 console_unlock 中触发 RCU 非法使用警告",
  "kernel_version": "3.3.0-rc2+",
  "phenomenon": "- 系统启动过程中测试 trace event 'cpu_idle' 时触发内核 WARNING。\n- 日志中出现 \"suspicious RCU usage\" 和 \"RCU used illegally from extended quiescent state\" 错误信息。\n- 调用栈显示警告路径为 `console_unlock` -> `vprintk` -> `__atomic_notifier_call_chain` -> `cpuidle_idle_call`。",
  "root_cause": "系统处于 extended quiescent state（扩展静默状态，如 idle loop）时，`cpuidle_idle_call` 流程中触发了 `console_unlock`，进而调用 `atomic_notifier_call_chain`。该操作尝试在 RCU 保护的 notifier chain 上执行 `rcu_dereference_check`，导致在非法上下文中使用 RCU。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "测试 cpu_idle trace event 时在 console_unlock 中触发 RCU 非法使用警告.txt",
  "content": "内核版本：3.3.0-rc2+\n\n## 问题现象\n- 系统启动过程中测试 trace event 'cpu_idle' 时触发内核 WARNING。\n- 日志中出现 \"suspicious RCU usage\" 和 \"RCU used illegally from extended quiescent state\" 错误信息。\n- 调用栈显示警告路径为 `console_unlock` -> `vprintk` -> `__atomic_notifier_call_chain` -> `cpuidle_idle_call`。\n\n## 问题根因\n系统处于 extended quiescent state（扩展静默状态，如 idle loop）时，`cpuidle_idle_call` 流程中触发了 `console_unlock`，进而调用 `atomic_notifier_call_chain`。该操作尝试在 RCU 保护的 notifier chain 上执行 `rcu_dereference_check`，导致在非法上下文中使用 RCU。\n\n## 解决方案\n暂无"
}
