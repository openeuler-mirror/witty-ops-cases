{
  "title": "BTRFS驱动未正确处理unpin_extent_cache返回值导致事务中止",
  "kernel_version": "5.5.9-200.fc31",
  "phenomenon": "- 内核日志打印 \"BTRFS: Transaction aborted (error -28)\"\n- 触发 WARNING，调用栈位于 `fs/btrfs/inode.c:3261 btrfs_finish_ordered_io`\n- 文件系统报错 \"errno=-28 No space left\" 并强制转为只读模式",
  "root_cause": "在 `btrfs_finish_ordered_io` 函数中调用 `unpin_extent_cache` 时，代码未将其返回值赋给 `ret` 变量，而是直接使用了 `ret` 的旧值进行判断。这导致 `unpin_extent_cache` 返回的实际错误码（如 -ENOSPC）被忽略，错误处理逻辑失效，进而触发事务中止。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "BTRFS驱动未正确处理unpin_extent_cache返回值导致事务中止.txt",
  "content": "内核版本：5.5.9-200.fc31\n\n## 问题现象\n- 内核日志打印 \"BTRFS: Transaction aborted (error -28)\"\n- 触发 WARNING，调用栈位于 `fs/btrfs/inode.c:3261 btrfs_finish_ordered_io`\n- 文件系统报错 \"errno=-28 No space left\" 并强制转为只读模式\n\n## 问题根因\n在 `btrfs_finish_ordered_io` 函数中调用 `unpin_extent_cache` 时，代码未将其返回值赋给 `ret` 变量，而是直接使用了 `ret` 的旧值进行判断。这导致 `unpin_extent_cache` 返回的实际错误码（如 -ENOSPC）被忽略，错误处理逻辑失效，进而触发事务中止。\n\n## 解决方案\n暂无"
}
