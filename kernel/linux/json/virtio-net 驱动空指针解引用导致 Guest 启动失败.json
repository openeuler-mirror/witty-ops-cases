{
  "title": "virtio-net 驱动空指针解引用导致 Guest 启动失败",
  "kernel_version": "6.3.0-rc7",
  "phenomenon": "- Guest 虚拟机无法正常启动，触发 kernel panic\n- 内核日志报错：\"BUG: kernel NULL pointer dereference, address: 0000000000000010\"\n- RIP 寄存器指向 `virtnet_set_guest_offloads+0x4c/0xc0 [virtio_net]`\n- 触发 Oops: 0002 [#1] PREEMPT SMP PTI",
  "root_cause": "当 QEMU 启动参数中禁用了 virtio-net 设备的控制队列相关选项（如 `ctrl_vq=off`）时，`virtio_net` 驱动在初始化（`virtnet_probe`）过程中调用 `virtnet_set_features` 函数，进而调用 `virtnet_set_guest_offloads`。该函数尝试访问未初始化或无效的内存指针（RAX 为 0），导致写入地址 0x10 时发生空指针解引用。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "virtio-net 驱动空指针解引用导致 Guest 启动失败.txt",
  "content": "内核版本：6.3.0-rc7\n\n## 问题现象\n- Guest 虚拟机无法正常启动，触发 kernel panic\n- 内核日志报错：\"BUG: kernel NULL pointer dereference, address: 0000000000000010\"\n- RIP 寄存器指向 `virtnet_set_guest_offloads+0x4c/0xc0 [virtio_net]`\n- 触发 Oops: 0002 [#1] PREEMPT SMP PTI\n\n## 问题根因\n当 QEMU 启动参数中禁用了 virtio-net 设备的控制队列相关选项（如 `ctrl_vq=off`）时，`virtio_net` 驱动在初始化（`virtnet_probe`）过程中调用 `virtnet_set_features` 函数，进而调用 `virtnet_set_guest_offloads`。该函数尝试访问未初始化或无效的内存指针（RAX 为 0），导致写入地址 0x10 时发生空指针解引用。\n\n## 解决方案\n暂无"
}
