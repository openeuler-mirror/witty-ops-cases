{
  "title": "AMDGPU驱动执行OpenCL任务时触发GPU重置导致黑屏",
  "kernel_version": "5.10.5-gentoo",
  "phenomenon": "- 运行 OpenCL 任务（如 Tesseract OCR）时屏幕黑屏，连接的显示器无信号，但系统网络仍可达。\n- 内核日志显示 `amdgpu_dm_atomic_commit_tail` 等待 fences 超时。\n- 触发 `ring gfx timeout`，导致 GPU reset 开始并成功执行。\n- GPU 重置过程中，函数 `dc_commit_state` 触发 WARNING。\n- 重置后 `amdgpu_ib_ring_tests` 报告 gfx ring 测试失败（错误码 -110）。",
  "root_cause": "GPU ring gfx 发生超时，触发 `amdgpu_job_timedout` 并启动 GPU reset 流程；在重置恢复阶段，`dc_commit_state` 函数触发了 WARNING，且 gfx ring 的 IB test 失败。具体导致 ring gfx 超时的根本原因未明确。",
  "solution": "暂无。评论中建议排查内核是否使用 clang 编译（可能导致兼容性问题），但报告者确认使用 gcc 编译，问题未解决。",
  "category": "kernel",
  "source_file": "AMDGPU驱动执行OpenCL任务时触发GPU重置导致黑屏.txt",
  "content": "内核版本：5.10.5-gentoo\n\n## 问题现象\n- 运行 OpenCL 任务（如 Tesseract OCR）时屏幕黑屏，连接的显示器无信号，但系统网络仍可达。\n- 内核日志显示 `amdgpu_dm_atomic_commit_tail` 等待 fences 超时。\n- 触发 `ring gfx timeout`，导致 GPU reset 开始并成功执行。\n- GPU 重置过程中，函数 `dc_commit_state` 触发 WARNING。\n- 重置后 `amdgpu_ib_ring_tests` 报告 gfx ring 测试失败（错误码 -110）。\n\n## 问题根因\nGPU ring gfx 发生超时，触发 `amdgpu_job_timedout` 并启动 GPU reset 流程；在重置恢复阶段，`dc_commit_state` 函数触发了 WARNING，且 gfx ring 的 IB test 失败。具体导致 ring gfx 超时的根本原因未明确。\n\n## 解决方案\n暂无。评论中建议排查内核是否使用 clang 编译（可能导致兼容性问题），但报告者确认使用 gcc 编译，问题未解决。"
}
