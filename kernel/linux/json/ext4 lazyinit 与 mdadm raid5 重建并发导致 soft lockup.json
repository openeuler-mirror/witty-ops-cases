{
  "title": "ext4 lazyinit 与 mdadm raid5 重建并发导致 soft lockup",
  "kernel_version": "3.13.10",
  "phenomenon": "- 在新创建的 mdadm raid5 阵列及 LUKS 加密层上创建 ext4 文件系统时，lazyinit 失败\n- 内核日志显示 lazyinit 线程出现 soft lockup 警告\n- jbd2 commit 线程长时间等待块 I/O 操作完成，无任何进展",
  "root_cause": "怀疑是 dm-crypt 与 md raid5 代码之间的交互问题，在特定的 I/O 模式（如 raid5 重建期间进行 rsync）下触发，导致块 I/O 请求无法完成，进而引发 ext4 层的等待超时与 soft lockup。",
  "solution": "暂无（问题无法稳定复现，Bug 状态为 NEW）。",
  "category": "kernel",
  "source_file": "ext4 lazyinit 与 mdadm raid5 重建并发导致 soft lockup.txt",
  "content": "内核版本：3.13.10\n\n## 问题现象\n- 在新创建的 mdadm raid5 阵列及 LUKS 加密层上创建 ext4 文件系统时，lazyinit 失败\n- 内核日志显示 lazyinit 线程出现 soft lockup 警告\n- jbd2 commit 线程长时间等待块 I/O 操作完成，无任何进展\n\n## 问题根因\n怀疑是 dm-crypt 与 md raid5 代码之间的交互问题，在特定的 I/O 模式（如 raid5 重建期间进行 rsync）下触发，导致块 I/O 请求无法完成，进而引发 ext4 层的等待超时与 soft lockup。\n\n## 解决方案\n暂无（问题无法稳定复现，Bug 状态为 NEW）。"
}
