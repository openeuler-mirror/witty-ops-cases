{
  "title": "PCMCIA pccardd线程非RUNNING状态下调用阻塞操作触发内核WARNING",
  "kernel_version": "3.19.0-rc1",
  "phenomenon": "- 两个 pccardd 进程 CPU 占用率达 100%。\n- 内核日志打印 WARNING：`do not call blocking ops when !TASK_RUNNING; state=1 set at pccardd`。\n- 调用栈显示 `__might_sleep` -> `mutex_lock_nested` -> `pccardd`。\n- 该问题为回归问题，在 3.18.1-2 版本中未出现。",
  "root_cause": "`pccardd` 线程在进程状态被设置为非 TASK_RUNNING（state=1）的情况下，调用了可能阻塞的 `mutex_lock_nested` 函数，违反了内核调度规则，从而触发 `__might_sleep` 检查告警。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "PCMCIA pccardd线程非RUNNING状态下调用阻塞操作触发内核WARNING.txt",
  "content": "内核版本：3.19.0-rc1\n\n## 问题现象\n- 两个 pccardd 进程 CPU 占用率达 100%。\n- 内核日志打印 WARNING：`do not call blocking ops when !TASK_RUNNING; state=1 set at pccardd`。\n- 调用栈显示 `__might_sleep` -> `mutex_lock_nested` -> `pccardd`。\n- 该问题为回归问题，在 3.18.1-2 版本中未出现。\n\n## 问题根因\n`pccardd` 线程在进程状态被设置为非 TASK_RUNNING（state=1）的情况下，调用了可能阻塞的 `mutex_lock_nested` 函数，违反了内核调度规则，从而触发 `__might_sleep` 检查告警。\n\n## 解决方案\n暂无"
}
