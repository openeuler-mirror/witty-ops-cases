{
  "title": "__task_pid_nr_ns函数空指针解引用触发general protection fault",
  "kernel_version": "v5.2-rc6",
  "phenomenon": "- 内核触发 general protection fault (GPF)，KASAN 提示可能由空指针解引用引起。\n- 故障发生在 `__task_pid_nr_ns+0x100/0x2e0` (kernel/pid.c:431)。\n- 调用栈显示异常路径为 `wait_task_zombie` -> `wait_consider_task` -> `do_wait` -> `kernel_wait4`。\n- 故障时 RBX 寄存器值为 0，RAX 寄存器显示 KASAN shadow 内存标记。",
  "root_cause": "在 `wait_task_zombie` 执行流程中，调用 `task_pid_vnr` 获取进程 PID 时，传入 `__task_pid_nr_ns` 的 task 结构体指针为 NULL（RBX 寄存器为 0），导致函数在访问 task 结构体成员偏移时触发空指针解引用。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "__task_pid_nr_ns函数空指针解引用触发general protection fault.txt",
  "content": "内核版本：v5.2-rc6\n\n## 问题现象\n- 内核触发 general protection fault (GPF)，KASAN 提示可能由空指针解引用引起。\n- 故障发生在 `__task_pid_nr_ns+0x100/0x2e0` (kernel/pid.c:431)。\n- 调用栈显示异常路径为 `wait_task_zombie` -> `wait_consider_task` -> `do_wait` -> `kernel_wait4`。\n- 故障时 RBX 寄存器值为 0，RAX 寄存器显示 KASAN shadow 内存标记。\n\n## 问题根因\n在 `wait_task_zombie` 执行流程中，调用 `task_pid_vnr` 获取进程 PID 时，传入 `__task_pid_nr_ns` 的 task 结构体指针为 NULL（RBX 寄存器为 0），导致函数在访问 task 结构体成员偏移时触发空指针解引用。\n\n## 解决方案\n暂无"
}
