{
  "title": "DRM驱动pipe参数类型变更导致Pre-Tesla GPU内核日志刷屏",
  "kernel_version": "4.3rc6",
  "phenomenon": "- 系统日志（dmesg）被大量 WARNING 信息刷屏，报错位置在 `drm_irq.c:924` 的 `drm_vblank_count_and_time` 函数。\n- 调用栈显示触发路径为 `nouveau_finish_page_flip` -> `drm_send_vblank_event`。\n- 部分发行版环境下伴随图形界面出现“静电”伪影。\n- 受影响硬件包括 ASUS A8JN 笔记本（配备 G72M / GeForce Go 7300 GPU）。",
  "root_cause": "Commit `cc1ef118fc099295ae6aabbacc8af94d8d8885eb` 将 `pipe` 变量类型修改为 `unsigned int`。对于 Pre-Tesla 架构的 GPU，驱动层传入的 crtcid 为 -1。当该值传入无符号类型的检查逻辑时，触发了内核警告。",
  "solution": "开发者提供了修复补丁（attachment 191861），通过修正 Pre-NV50 架构的 pageflip 事件处理逻辑解决此问题。经测试验证该补丁可消除警告刷屏及图形伪影。",
  "category": "kernel",
  "source_file": "DRM驱动pipe参数类型变更导致Pre-Tesla GPU内核日志刷屏.txt",
  "content": "内核版本：4.3rc6\n\n## 问题现象\n- 系统日志（dmesg）被大量 WARNING 信息刷屏，报错位置在 `drm_irq.c:924` 的 `drm_vblank_count_and_time` 函数。\n- 调用栈显示触发路径为 `nouveau_finish_page_flip` -> `drm_send_vblank_event`。\n- 部分发行版环境下伴随图形界面出现“静电”伪影。\n- 受影响硬件包括 ASUS A8JN 笔记本（配备 G72M / GeForce Go 7300 GPU）。\n\n## 问题根因\nCommit `cc1ef118fc099295ae6aabbacc8af94d8d8885eb` 将 `pipe` 变量类型修改为 `unsigned int`。对于 Pre-Tesla 架构的 GPU，驱动层传入的 crtcid 为 -1。当该值传入无符号类型的检查逻辑时，触发了内核警告。\n\n## 解决方案\n开发者提供了修复补丁（attachment 191861），通过修正 Pre-NV50 架构的 pageflip 事件处理逻辑解决此问题。经测试验证该补丁可消除警告刷屏及图形伪影。"
}
