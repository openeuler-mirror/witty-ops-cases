{
  "title": "sm8150平台UFS初始化因gcc_ufs_phy_axi_clk时钟状态卡死导致启动失败",
  "kernel_version": "6.7.0",
  "phenomenon": "- sm8150 设备上 UFS 存储无法正常启动。\n- 内核日志报错：`gcc_ufs_phy_axi_clk status stuck at 'off'`。\n- 触发内核 WARNING，位于 `drivers/clk/qcom/clk-branch.c:86` 的 `clk_branch_wait` 函数中。\n- 调用栈显示：`clk_branch_wait` <- `clk_branch2_enable` <- `ufshcd_setup_clocks` <- `ufshcd_init` <- `ufs_qcom_probe`。",
  "root_cause": "在 UFS 驱动初始化过程中，时钟框架尝试使能 `gcc_ufs_phy_axi_clk` 时钟。函数 `clk_branch_wait` 在等待硬件时钟状态位变为 'on' 时超时，状态卡死在 'off'，导致使能操作失败并触发 WARNING。这表明底层硬件时钟状态未按预期翻转，可能涉及时钟树依赖配置或硬件状态异常。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "sm8150平台UFS初始化因gcc_ufs_phy_axi_clk时钟状态卡死导致启动失败.txt",
  "content": "内核版本：6.7.0\n\n## 问题现象\n- sm8150 设备上 UFS 存储无法正常启动。\n- 内核日志报错：`gcc_ufs_phy_axi_clk status stuck at 'off'`。\n- 触发内核 WARNING，位于 `drivers/clk/qcom/clk-branch.c:86` 的 `clk_branch_wait` 函数中。\n- 调用栈显示：`clk_branch_wait` <- `clk_branch2_enable` <- `ufshcd_setup_clocks` <- `ufshcd_init` <- `ufs_qcom_probe`。\n\n## 问题根因\n在 UFS 驱动初始化过程中，时钟框架尝试使能 `gcc_ufs_phy_axi_clk` 时钟。函数 `clk_branch_wait` 在等待硬件时钟状态位变为 'on' 时超时，状态卡死在 'off'，导致使能操作失败并触发 WARNING。这表明底层硬件时钟状态未按预期翻转，可能涉及时钟树依赖配置或硬件状态异常。\n\n## 解决方案\n暂无"
}
