{
  "title": "Nouveau驱动在混合显卡模式下DP-MST探测触发空指针解引用导致内核Oops",
  "kernel_version": "4.11.0-1-amd64 (Debian 4.11.6-1)",
  "phenomenon": "- 在ThinkPad P51混合显卡模式下使用Nouveau驱动连接扩展坞显示器时，系统触发内核Oops。\n- 日志显示 `WARNING: CPU: 7 PID: 3782 at ... nouveau_bo_del_ttm`。\n- 随后触发 `Oops: 0000 [#1]`，RIP 寄存器指向 `drm_fb_helper_add_one_connector+0x17/0xd0 [drm_kms_helper]`。\n- Call Trace 显示调用链为 `drm_dp_mst_link_probe_work` -> `drm_dp_add_port` -> `nv50_mstm_register_connector`。",
  "root_cause": "在DP-MST（Multi-Stream Transport）链路探测过程中，`drm_fb_helper_add_one_connector` 函数接收到了空指针参数（RDI寄存器为0），导致访问偏移量0x8时发生页面错误（CR2=0x8）。该问题发生在内核工作队列 `events_long` 处理 `drm_dp_mst_link_probe_work` 时。",
  "solution": "暂无内核补丁信息。规避方法：在BIOS中将显卡模式设置为“独立显卡”可避免此问题。相关问题追踪见 https://bugs.freedesktop.org/show_bug.cgi?id=101778",
  "category": "kernel",
  "source_file": "Nouveau驱动在混合显卡模式下DP-MST探测触发空指针解引用导致内核Oops.txt",
  "content": "内核版本：4.11.0-1-amd64 (Debian 4.11.6-1)\n\n## 问题现象\n- 在ThinkPad P51混合显卡模式下使用Nouveau驱动连接扩展坞显示器时，系统触发内核Oops。\n- 日志显示 `WARNING: CPU: 7 PID: 3782 at ... nouveau_bo_del_ttm`。\n- 随后触发 `Oops: 0000 [#1]`，RIP 寄存器指向 `drm_fb_helper_add_one_connector+0x17/0xd0 [drm_kms_helper]`。\n- Call Trace 显示调用链为 `drm_dp_mst_link_probe_work` -> `drm_dp_add_port` -> `nv50_mstm_register_connector`。\n\n## 问题根因\n在DP-MST（Multi-Stream Transport）链路探测过程中，`drm_fb_helper_add_one_connector` 函数接收到了空指针参数（RDI寄存器为0），导致访问偏移量0x8时发生页面错误（CR2=0x8）。该问题发生在内核工作队列 `events_long` 处理 `drm_dp_mst_link_probe_work` 时。\n\n## 解决方案\n暂无内核补丁信息。规避方法：在BIOS中将显卡模式设置为“独立显卡”可避免此问题。相关问题追踪见 https://bugs.freedesktop.org/show_bug.cgi?id=101778"
}
