{
  "title": "asus-nb-wmi驱动注册无效背光接口导致亮度调节失效及恢复黑屏",
  "kernel_version": "3.8.1",
  "phenomenon": "- 亮度调节按键无响应，无法通过系统命令调节屏幕亮度。\n- 系统从挂起状态恢复后屏幕黑屏（背光未开启），需手动合上并重新打开笔记本盖子才能点亮屏幕。\n- `/sys/class/backlight` 目录下存在 `asus-nb-wmi` 和 `intel_backlight` 两个背光控制接口，系统优先使用了无效的 `asus-nb-wmi` 接口。",
  "root_cause": "`asus-nb-wmi` 驱动程序错误地注册了一个损坏的背光控制接口，导致与有效的 `intel_backlight` 接口冲突，系统默认使用了该无效接口进行亮度控制。",
  "solution": "临时规避方法：\n1. 在 xorg.conf 中配置 backlight 选项指定使用正确的背光接口。\n2. 禁用 `asus-nb-wmi` 驱动模块。\n该问题在报告时尚未修复。",
  "category": "kernel",
  "source_file": "asus-nb-wmi驱动注册无效背光接口导致亮度调节失效及恢复黑屏.txt",
  "content": "内核版本：3.8.1\n\n## 问题现象\n- 亮度调节按键无响应，无法通过系统命令调节屏幕亮度。\n- 系统从挂起状态恢复后屏幕黑屏（背光未开启），需手动合上并重新打开笔记本盖子才能点亮屏幕。\n- `/sys/class/backlight` 目录下存在 `asus-nb-wmi` 和 `intel_backlight` 两个背光控制接口，系统优先使用了无效的 `asus-nb-wmi` 接口。\n\n## 问题根因\n`asus-nb-wmi` 驱动程序错误地注册了一个损坏的背光控制接口，导致与有效的 `intel_backlight` 接口冲突，系统默认使用了该无效接口进行亮度控制。\n\n## 解决方案\n临时规避方法：\n1. 在 xorg.conf 中配置 backlight 选项指定使用正确的背光接口。\n2. 禁用 `asus-nb-wmi` 驱动模块。\n该问题在报告时尚未修复。"
}
