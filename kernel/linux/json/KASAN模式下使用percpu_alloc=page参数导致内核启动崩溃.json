{
  "title": "KASAN模式下使用percpu_alloc=page参数导致内核启动崩溃",
  "kernel_version": "5.7.0",
  "phenomenon": "- 内核启用 KASAN 并添加 `percpu_alloc=page` 启动参数后，系统无法正常启动。\n- 日志显示 `BUG: unable to handle page fault for address: fffff52000000000`，错误码为 `not-present page`。\n- 触发 Oops，RIP 指向 `check_memory_region` 函数（位于 `mm/kasan/generic.c`）。\n- Call Trace 显示调用链为：`start_kernel` -> `setup_per_cpu_areas` -> `pcpu_page_first_chunk` -> `memcpy`。",
  "root_cause": "在 `percpu_alloc=page` 模式下，`pcpu_page_first_chunk` 函数执行 `memcpy` 操作时，KASAN 机制尝试检查内存区域，但访问了无效的影子内存地址（Shadow Memory），导致触发 page fault。",
  "solution": "暂无（Bug 状态为 NEW，尚未修复）。",
  "category": "kernel",
  "source_file": "KASAN模式下使用percpu_alloc=page参数导致内核启动崩溃.txt",
  "content": "内核版本：5.7.0\n\n## 问题现象\n- 内核启用 KASAN 并添加 `percpu_alloc=page` 启动参数后，系统无法正常启动。\n- 日志显示 `BUG: unable to handle page fault for address: fffff52000000000`，错误码为 `not-present page`。\n- 触发 Oops，RIP 指向 `check_memory_region` 函数（位于 `mm/kasan/generic.c`）。\n- Call Trace 显示调用链为：`start_kernel` -> `setup_per_cpu_areas` -> `pcpu_page_first_chunk` -> `memcpy`。\n\n## 问题根因\n在 `percpu_alloc=page` 模式下，`pcpu_page_first_chunk` 函数执行 `memcpy` 操作时，KASAN 机制尝试检查内存区域，但访问了无效的影子内存地址（Shadow Memory），导致触发 page fault。\n\n## 解决方案\n暂无（Bug 状态为 NEW，尚未修复）。"
}
