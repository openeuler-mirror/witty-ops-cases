{
  "title": "expire_timers访问已破坏的定时器链表导致内核Oops",
  "kernel_version": "4.19.73, 5.4.55",
  "phenomenon": "- 系统在运行图形任务或处理软中断时触发 Kernel Oops，日志显示 \"Unable to handle kernel paging request at virtual address dead000000000...\"（该地址为 LIST_POISON2）。\n- 调用栈显示崩溃发生在 `expire_timers` 函数中，由 `run_timer_softirq` 调用。\n- 部分场景下系统触发 Kernel panic 并显示 \"Fatal exception in interrupt\"。",
  "root_cause": "内核代码访问了 `LIST_POISON2` 地址（如 `dead000000000208`），这是内核用于标记已删除链表节点的特殊值。根据维护者分析，这表明某个定时器在处于排队状态时被外部代码错误地修改或释放，导致链表结构损坏。当定时器软中断尝试处理该定时器时，访问了无效的链表指针从而触发 Oops。具体的故障模块尚未确定。",
  "solution": "暂无正式修复补丁。建议开启以下内核调试选项以定位破坏定时器的具体代码：\n- CONFIG_DEBUG_OBJECTS\n- CONFIG_DEBUG_OBJECTS_ENABLE_DEFAULT=1\n- CONFIG_DEBUG_OBJECTS_FREE\n- CONFIG_DEBUG_OBJECTS_TIMERS\n- CONFIG_DEBUG_OBJECTS_WORK",
  "category": "kernel",
  "source_file": "expire_timers访问已破坏的定时器链表导致内核Oops.txt",
  "content": "内核版本：4.19.73, 5.4.55\n\n## 问题现象\n- 系统在运行图形任务或处理软中断时触发 Kernel Oops，日志显示 \"Unable to handle kernel paging request at virtual address dead000000000...\"（该地址为 LIST_POISON2）。\n- 调用栈显示崩溃发生在 `expire_timers` 函数中，由 `run_timer_softirq` 调用。\n- 部分场景下系统触发 Kernel panic 并显示 \"Fatal exception in interrupt\"。\n\n## 问题根因\n内核代码访问了 `LIST_POISON2` 地址（如 `dead000000000208`），这是内核用于标记已删除链表节点的特殊值。根据维护者分析，这表明某个定时器在处于排队状态时被外部代码错误地修改或释放，导致链表结构损坏。当定时器软中断尝试处理该定时器时，访问了无效的链表指针从而触发 Oops。具体的故障模块尚未确定。\n\n## 解决方案\n暂无正式修复补丁。建议开启以下内核调试选项以定位破坏定时器的具体代码：\n- CONFIG_DEBUG_OBJECTS\n- CONFIG_DEBUG_OBJECTS_ENABLE_DEFAULT=1\n- CONFIG_DEBUG_OBJECTS_FREE\n- CONFIG_DEBUG_OBJECTS_TIMERS\n- CONFIG_DEBUG_OBJECTS_WORK"
}
