{
  "title": "init_files 结构体 resize_wait 未初始化导致内核空指针崩溃",
  "kernel_version": "4.14.41",
  "phenomenon": "- 在内核线程中调用 `get_unused_fd_flags` 获取文件描述符时触发内核崩溃。\n- 系统日志显示 \"BUG: unable to handle kernel NULL pointer dereference at (null)\"。\n- 故障调用栈为：`__wake_up_common` <- `__wake_up` <- `expand_files` <- `__alloc_fd` <- `get_unused_fd_flags`。",
  "root_cause": "`init_files` 结构体中的成员 `resize_wait` 在使用前未被初始化。当 `expand_files` 函数执行扩容操作并调用 `__wake_up` 唤醒等待队列时，访问了该未初始化的指针，从而触发空指针解引用。",
  "solution": "修改 `fs/file.c` 文件，在 `init_files` 结构体定义中显式初始化 `resize_wait` 成员（添加 `.resize_wait = __WAIT_QUEUE_HEAD_INITIALIZER(init_files.resize_wait)`）。该补丁已被维护者接收。",
  "category": "kernel",
  "source_file": "init_files 结构体 resize_wait 未初始化导致内核空指针崩溃.txt",
  "content": "内核版本：4.14.41\n\n## 问题现象\n- 在内核线程中调用 `get_unused_fd_flags` 获取文件描述符时触发内核崩溃。\n- 系统日志显示 \"BUG: unable to handle kernel NULL pointer dereference at (null)\"。\n- 故障调用栈为：`__wake_up_common` <- `__wake_up` <- `expand_files` <- `__alloc_fd` <- `get_unused_fd_flags`。\n\n## 问题根因\n`init_files` 结构体中的成员 `resize_wait` 在使用前未被初始化。当 `expand_files` 函数执行扩容操作并调用 `__wake_up` 唤醒等待队列时，访问了该未初始化的指针，从而触发空指针解引用。\n\n## 解决方案\n修改 `fs/file.c` 文件，在 `init_files` 结构体定义中显式初始化 `resize_wait` 成员（添加 `.resize_wait = __WAIT_QUEUE_HEAD_INITIALIZER(init_files.resize_wait)`）。该补丁已被维护者接收。"
}
