{
  "title": "CIFS模块strncpy缓冲区溢出触发fortify_panic导致内核崩溃",
  "kernel_version": "5.15.0-60-generic",
  "phenomenon": "- 系统触发 kernel BUG，日志显示 \"kernel BUG at lib/string.c:1165!\"\n- CPU 发生 invalid opcode 异常（invalid opcode: 0000 [#1]）\n- 日志末尾明确检测到 \"detected buffer overflow in strncpy\"\n- Call Trace 显示调用链为 `__x64_sys_setxattr` -> `cifs_xattr_set` -> `CIFSSMBSetEA.cold` -> `fortify_panic`",
  "root_cause": "CIFS 模块在处理 setxattr 系统调用时，`CIFSSMBSetEA` 函数内部执行 `strncpy` 操作发生缓冲区溢出，触发了内核 FORTIFY_SOURCE 安全保护机制，进而调用 `fortify_panic` 导致内核崩溃。",
  "solution": "暂无（Bug 状态为 NEW，建议尝试升级至最新内核版本验证）。",
  "category": "kernel",
  "source_file": "CIFS模块strncpy缓冲区溢出触发fortify_panic导致内核崩溃.txt",
  "content": "内核版本：5.15.0-60-generic\n\n## 问题现象\n- 系统触发 kernel BUG，日志显示 \"kernel BUG at lib/string.c:1165!\"\n- CPU 发生 invalid opcode 异常（invalid opcode: 0000 [#1]）\n- 日志末尾明确检测到 \"detected buffer overflow in strncpy\"\n- Call Trace 显示调用链为 `__x64_sys_setxattr` -> `cifs_xattr_set` -> `CIFSSMBSetEA.cold` -> `fortify_panic`\n\n## 问题根因\nCIFS 模块在处理 setxattr 系统调用时，`CIFSSMBSetEA` 函数内部执行 `strncpy` 操作发生缓冲区溢出，触发了内核 FORTIFY_SOURCE 安全保护机制，进而调用 `fortify_panic` 导致内核崩溃。\n\n## 解决方案\n暂无（Bug 状态为 NEW，建议尝试升级至最新内核版本验证）。"
}
