{
  "title": "IPsec ESP性能在内核4.12及以上版本因TSO处理逻辑错误急剧下降",
  "kernel_version": ">=4.12",
  "phenomenon": "- IPsec (ESP) 隧道吞吐量从约 700Mbps 急剧下降至约 500Kbps~1.5Mbps。\n- 问题出现在内核版本 4.12 及以上，回退至 4.10 版本可恢复正常。\n- 禁用 TSO (TCP Segmentation Offload) 可恢复吞吐量，但会导致 ksoftirqd 进程 CPU 占用率显著升高。",
  "root_cause": "在 `net/xfrm/xfrm_output.c` 代码逻辑中，`xfrm_output` 函数过早地将 `skb->encapsulation` 标志位清零。这导致在 IPsec 软件加密路径下，GSO/TSO 处理逻辑无法正确识别封装状态，引发网络吞吐性能瓶颈。该问题可能与 commit d77e38e612a0 相关。",
  "solution": "应用补丁修改 `net/xfrm/xfrm_output.c`，将 `skb->encapsulation = 0` 操作从 `xfrm_output` 函数移至 `xfrm_output_one` 函数内部（非 offload 路径下执行）。\n临时规避方案：通过 `ethtool -K <interface> tso off` 禁用 TSO 功能。",
  "category": "kernel",
  "source_file": "IPsec ESP性能在内核4.12及以上版本因TSO处理逻辑错误急剧下降.txt",
  "content": "内核版本：>=4.12\n\n## 问题现象\n- IPsec (ESP) 隧道吞吐量从约 700Mbps 急剧下降至约 500Kbps~1.5Mbps。\n- 问题出现在内核版本 4.12 及以上，回退至 4.10 版本可恢复正常。\n- 禁用 TSO (TCP Segmentation Offload) 可恢复吞吐量，但会导致 ksoftirqd 进程 CPU 占用率显著升高。\n\n## 问题根因\n在 `net/xfrm/xfrm_output.c` 代码逻辑中，`xfrm_output` 函数过早地将 `skb->encapsulation` 标志位清零。这导致在 IPsec 软件加密路径下，GSO/TSO 处理逻辑无法正确识别封装状态，引发网络吞吐性能瓶颈。该问题可能与 commit d77e38e612a0 相关。\n\n## 解决方案\n应用补丁修改 `net/xfrm/xfrm_output.c`，将 `skb->encapsulation = 0` 操作从 `xfrm_output` 函数移至 `xfrm_output_one` 函数内部（非 offload 路径下执行）。\n临时规避方案：通过 `ethtool -K <interface> tso off` 禁用 TSO 功能。"
}
