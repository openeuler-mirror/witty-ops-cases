{
  "title": "NVMe驱动在IO请求处理路径触发kernel BUG导致系统崩溃",
  "kernel_version": "4.2.2-1-pve SMP",
  "phenomenon": "- 系统触发 kernel BUG，日志显示 \"kernel BUG at drivers/block/nvme-core.c:721!\"\n- CPU 抛出 invalid opcode 异常（0000 [#1]），系统状态标记为 Tainted: P IO\n- 故障发生在 KVM 进程上下文中，调用栈显示由 `SyS_io_submit` -> `blk_mq_run_hw_queue` -> `nvme_queue_rq` 触发",
  "root_cause": "NVMe 驱动在 `nvme_queue_rq` 函数执行过程中，触发了位于 `drivers/block/nvme-core.c:721` 处的 `BUG()` 断言失败。具体的逻辑触发条件或代码缺陷原因未明确。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "NVMe驱动在IO请求处理路径触发kernel BUG导致系统崩溃.txt",
  "content": "内核版本：4.2.2-1-pve SMP\n\n## 问题现象\n- 系统触发 kernel BUG，日志显示 \"kernel BUG at drivers/block/nvme-core.c:721!\"\n- CPU 抛出 invalid opcode 异常（0000 [#1]），系统状态标记为 Tainted: P IO\n- 故障发生在 KVM 进程上下文中，调用栈显示由 `SyS_io_submit` -> `blk_mq_run_hw_queue` -> `nvme_queue_rq` 触发\n\n## 问题根因\nNVMe 驱动在 `nvme_queue_rq` 函数执行过程中，触发了位于 `drivers/block/nvme-core.c:721` 处的 `BUG()` 断言失败。具体的逻辑触发条件或代码缺陷原因未明确。\n\n## 解决方案\n暂无"
}
