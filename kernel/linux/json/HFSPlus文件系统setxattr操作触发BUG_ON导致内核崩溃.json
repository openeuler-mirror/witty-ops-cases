{
  "title": "HFSPlus文件系统setxattr操作触发BUG_ON导致内核崩溃",
  "kernel_version": "4.18.0-rc1+",
  "phenomenon": "- 系统触发 kernel panic，日志显示 \"invalid opcode: 0000 [#1]\"。\n- 内核报错信息：`kernel BUG at fs/hfsplus/xattr.c:181!`。\n- 故障发生在挂载精心构造的 HFS+ 镜像并执行 `setxattr` 系统调用时。\n- RIP 寄存器指向 `__hfsplus_setxattr+0x1086/0x1160`。",
  "root_cause": "在 `fs/hfsplus/xattr.c` 文件的第 181 行存在断言检查 `BUG_ON(i_size_read(attr_file) != 0)`。该断言要求属性文件的大小必须为零。由于精心构造的镜像导致属性文件大小非零，断言失败，内核调用 `BUG()` 触发 panic。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "HFSPlus文件系统setxattr操作触发BUG_ON导致内核崩溃.txt",
  "content": "内核版本：4.18.0-rc1+\n\n## 问题现象\n- 系统触发 kernel panic，日志显示 \"invalid opcode: 0000 [#1]\"。\n- 内核报错信息：`kernel BUG at fs/hfsplus/xattr.c:181!`。\n- 故障发生在挂载精心构造的 HFS+ 镜像并执行 `setxattr` 系统调用时。\n- RIP 寄存器指向 `__hfsplus_setxattr+0x1086/0x1160`。\n\n## 问题根因\n在 `fs/hfsplus/xattr.c` 文件的第 181 行存在断言检查 `BUG_ON(i_size_read(attr_file) != 0)`。该断言要求属性文件的大小必须为零。由于精心构造的镜像导致属性文件大小非零，断言失败，内核调用 `BUG()` 触发 panic。\n\n## 解决方案\n暂无"
}
