{
  "title": "Device-mapper raid1 无元数据设备写入时触发死锁挂起",
  "kernel_version": "4.15",
  "phenomenon": "- 使用 device-mapper 创建无元数据设备的 raid1 target 后，首次写入操作导致系统挂起。\n- `/proc/diskstats` 显示 I/O 请求处于 in-flight 状态无法完成。\n- 执行 `dmsetup remove` 或 `dmsetup suspend` 等操作也会挂起。",
  "root_cause": "在无元数据设备的 raid1 配置中，不存在 raid superblock，但 `md.c` 模块中的 `md_write_start()` 函数仍尝试调度等待 superblock 写入完成，导致进程在 `wait_event` 处无限等待，引发死锁。",
  "solution": "开发者已提交修复补丁至社区邮件列表。修复思路是在 `md_write_start()` 函数中增加判断，若设备无 superblock（即 `rdev->sb_page` 为空），则跳过等待逻辑直接返回。",
  "category": "kernel",
  "source_file": "Device-mapper raid1 无元数据设备写入时触发死锁挂起.txt",
  "content": "内核版本：4.15\n\n## 问题现象\n- 使用 device-mapper 创建无元数据设备的 raid1 target 后，首次写入操作导致系统挂起。\n- `/proc/diskstats` 显示 I/O 请求处于 in-flight 状态无法完成。\n- 执行 `dmsetup remove` 或 `dmsetup suspend` 等操作也会挂起。\n\n## 问题根因\n在无元数据设备的 raid1 配置中，不存在 raid superblock，但 `md.c` 模块中的 `md_write_start()` 函数仍尝试调度等待 superblock 写入完成，导致进程在 `wait_event` 处无限等待，引发死锁。\n\n## 解决方案\n开发者已提交修复补丁至社区邮件列表。修复思路是在 `md_write_start()` 函数中增加判断，若设备无 superblock（即 `rdev->sb_page` 为空），则跳过等待逻辑直接返回。"
}
