{
  "title": "XFS日志恢复期间xfs_bmapi_remap触发断言失败",
  "kernel_version": "5.14.0-rc4+",
  "phenomenon": "- 运行 xfstests 测试集 xfs/319 失败，报告文件系统不一致。\n- 系统日志显示 XFS 断言失败：`XFS: Assertion failed: got.br_startoff > bno, file: fs/xfs/libxfs/xfs_bmap.c, line: 4715`。\n- 触发内核 WARNING，调用栈显示进程为 mount，处于日志恢复流程中。",
  "root_cause": "在执行错误注入测试后，文件系统进行日志恢复。在处理 BUI（BMap Intent）项时，函数调用链为 `xfs_bui_item_recover` -> `xfs_bmap_finish_one` -> `xfs_bmapi_remap`。在 `xfs_bmapi_remap` 函数中，代码断言 `got.br_startoff > bno` 失败，表明在执行 remap 操作时，查找到的 extent 起始偏移量与预期不符，触发了内核 WARNING。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "XFS日志恢复期间xfs_bmapi_remap触发断言失败.txt",
  "content": "内核版本：5.14.0-rc4+\n\n## 问题现象\n- 运行 xfstests 测试集 xfs/319 失败，报告文件系统不一致。\n- 系统日志显示 XFS 断言失败：`XFS: Assertion failed: got.br_startoff > bno, file: fs/xfs/libxfs/xfs_bmap.c, line: 4715`。\n- 触发内核 WARNING，调用栈显示进程为 mount，处于日志恢复流程中。\n\n## 问题根因\n在执行错误注入测试后，文件系统进行日志恢复。在处理 BUI（BMap Intent）项时，函数调用链为 `xfs_bui_item_recover` -> `xfs_bmap_finish_one` -> `xfs_bmapi_remap`。在 `xfs_bmapi_remap` 函数中，代码断言 `got.br_startoff > bno` 失败，表明在执行 remap 操作时，查找到的 extent 起始偏移量与预期不符，触发了内核 WARNING。\n\n## 解决方案\n暂无"
}
