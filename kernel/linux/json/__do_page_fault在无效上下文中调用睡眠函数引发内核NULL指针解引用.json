{
  "title": "__do_page_fault在无效上下文中调用睡眠函数引发内核NULL指针解引用",
  "kernel_version": "5.3.10",
  "phenomenon": "- 内核日志报告 \"BUG: sleeping function called from invalid context in __do_page_fault\"，位置在 arch/x86/mm/fault.c。\n- 上下文检查显示 irqs_disabled(): 1，表明在禁用中断的上下文中调用了睡眠函数。\n- 随后触发 \"BUG: kernel NULL pointer dereference\"，访问地址为 00000000000000e4，导致内核 Oops (0002)。\n- Call Trace 指向：__do_page_fault -> async_page_fault -> do_syscall_64。",
  "root_cause": "在 arch/x86/mm/fault.c 的 __do_page_fault 函数处理路径中，代码在 IRQ 中断被禁用的无效上下文中调用了可能睡眠的函数。这违反了内核原子上下文的编程规则，导致系统状态异常并最终引发 NULL 指针解引用错误。",
  "solution": "暂无（Bug 状态为 NEW，尚未修复）。",
  "category": "kernel",
  "source_file": "__do_page_fault在无效上下文中调用睡眠函数引发内核NULL指针解引用.txt",
  "content": "内核版本：5.3.10\n\n## 问题现象\n- 内核日志报告 \"BUG: sleeping function called from invalid context in __do_page_fault\"，位置在 arch/x86/mm/fault.c。\n- 上下文检查显示 irqs_disabled(): 1，表明在禁用中断的上下文中调用了睡眠函数。\n- 随后触发 \"BUG: kernel NULL pointer dereference\"，访问地址为 00000000000000e4，导致内核 Oops (0002)。\n- Call Trace 指向：__do_page_fault -> async_page_fault -> do_syscall_64。\n\n## 问题根因\n在 arch/x86/mm/fault.c 的 __do_page_fault 函数处理路径中，代码在 IRQ 中断被禁用的无效上下文中调用了可能睡眠的函数。这违反了内核原子上下文的编程规则，导致系统状态异常并最终引发 NULL 指针解引用错误。\n\n## 解决方案\n暂无（Bug 状态为 NEW，尚未修复）。"
}
