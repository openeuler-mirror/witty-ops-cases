{
  "title": "进程退出阶段在exit_sem获取自旋锁触发soft lockup",
  "kernel_version": "4.2.3",
  "phenomenon": "- httpd进程异常，表现为内核线程形式（名称变为`[httpd]`），忽略所有信号，CPU时间持续累积。\n- 系统日志报错：\"NMI watchdog: BUG: soft lockup - CPU#7 stuck for 22s!\"。\n- 触发RCU stall告警：\"rcu_sched self-detected stall on CPU\"。\n- 堆栈信息显示进程卡在 `do_exit` -> `exit_sem` -> `_raw_spin_lock` 流程中。",
  "root_cause": "进程在退出过程中执行 `exit_sem` 函数时，试图获取自旋锁 `_raw_spin_lock` 失败并陷入无限循环，导致CPU长时间被占用无法释放，触发soft lockup。",
  "solution": "暂无（Bug状态为NEW，报告中未提供修复补丁，仅通过重启服务器恢复）。",
  "category": "kernel",
  "source_file": "进程退出阶段在exit_sem获取自旋锁触发soft lockup.txt",
  "content": "内核版本：4.2.3\n\n## 问题现象\n- httpd进程异常，表现为内核线程形式（名称变为`[httpd]`），忽略所有信号，CPU时间持续累积。\n- 系统日志报错：\"NMI watchdog: BUG: soft lockup - CPU#7 stuck for 22s!\"。\n- 触发RCU stall告警：\"rcu_sched self-detected stall on CPU\"。\n- 堆栈信息显示进程卡在 `do_exit` -> `exit_sem` -> `_raw_spin_lock` 流程中。\n\n## 问题根因\n进程在退出过程中执行 `exit_sem` 函数时，试图获取自旋锁 `_raw_spin_lock` 失败并陷入无限循环，导致CPU长时间被占用无法释放，触发soft lockup。\n\n## 解决方案\n暂无（Bug状态为NEW，报告中未提供修复补丁，仅通过重启服务器恢复）。"
}
