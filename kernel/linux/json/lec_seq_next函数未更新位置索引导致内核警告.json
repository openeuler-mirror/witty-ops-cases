{
  "title": "lec_seq_next函数未更新位置索引导致内核警告",
  "kernel_version": "2.6.12 及以上版本",
  "phenomenon": "- 使用 stress-ng 对 /procfs 进行压力测试时，读取 /proc/net/atm/lec 触发内核警告：\"seq_file: buggy .next function lec_seq_next [lec] did not update position index\"。\n- 可通过命令 `dd if=/proc/net/atm/lec seek=1 of=/dev/stdout bs=1K` 复现该问题。\n- 读取该 proc 文件时会出现异常阻塞。",
  "root_cause": "`lec` 模块中的 `lec_seq_next` 函数实现存在缺陷，在迭代过程中未更新位置索引，违反了 seq_file 接口的要求。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "lec_seq_next函数未更新位置索引导致内核警告.txt",
  "content": "内核版本：2.6.12 及以上版本\n\n## 问题现象\n- 使用 stress-ng 对 /procfs 进行压力测试时，读取 /proc/net/atm/lec 触发内核警告：\"seq_file: buggy .next function lec_seq_next [lec] did not update position index\"。\n- 可通过命令 `dd if=/proc/net/atm/lec seek=1 of=/dev/stdout bs=1K` 复现该问题。\n- 读取该 proc 文件时会出现异常阻塞。\n\n## 问题根因\n`lec` 模块中的 `lec_seq_next` 函数实现存在缺陷，在迭代过程中未更新位置索引，违反了 seq_file 接口的要求。\n\n## 解决方案\n暂无"
}
