{
  "title": "DM thin-provisioning 元数据提交失败后访问损坏的 btree",
  "kernel_version": "6.1.0-rc5",
  "phenomenon": "- 元数据提交操作 `dm_pool_commit_metadata` 失败，返回错误码 -5\n- 内核日志显示 \"aborting current metadata transaction\"\n- 随后系统访问了损坏的 btree 节点，日志显示 `pmd->root` 指向的 block 31 为过期节点，`nr_entries` 数值异常，且包含大量无效 value 值\n- 触发 EXT4-fs I/O error 和 Buffer I/O error",
  "root_cause": "在 DM thin-provisioning 模块中，当 `dm_pool_commit_metadata` 提交失败并执行事务中止逻辑时，元数据状态未正确回滚或恢复，导致 `pmd->root` 指针指向了已损坏或过期的 btree 节点，后续对该节点的访问读取到了无效数据。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "DM thin-provisioning 元数据提交失败后访问损坏的 btree.txt",
  "content": "内核版本：6.1.0-rc5\n\n## 问题现象\n- 元数据提交操作 `dm_pool_commit_metadata` 失败，返回错误码 -5\n- 内核日志显示 \"aborting current metadata transaction\"\n- 随后系统访问了损坏的 btree 节点，日志显示 `pmd->root` 指向的 block 31 为过期节点，`nr_entries` 数值异常，且包含大量无效 value 值\n- 触发 EXT4-fs I/O error 和 Buffer I/O error\n\n## 问题根因\n在 DM thin-provisioning 模块中，当 `dm_pool_commit_metadata` 提交失败并执行事务中止逻辑时，元数据状态未正确回滚或恢复，导致 `pmd->root` 指针指向了已损坏或过期的 btree 节点，后续对该节点的访问读取到了无效数据。\n\n## 解决方案\n暂无"
}
