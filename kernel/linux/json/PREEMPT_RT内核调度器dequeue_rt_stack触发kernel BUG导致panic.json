{
  "title": "PREEMPT_RT内核调度器dequeue_rt_stack触发kernel BUG导致panic",
  "kernel_version": "6.12.13-talos, 6.12.18-mxie (PREEMPT_RT)",
  "phenomenon": "- 系统运行 PREEMPT_RT 内核时每天随机崩溃一到两次。\n- 日志显示 \"kernel BUG at kernel/sched/rt.c:1035!\"，伴随 \"Oops: invalid opcode\"。\n- Call Trace 显示崩溃路径为：dequeue_rt_stack -> enqueue_task_rt -> enqueue_task -> try_to_wake_up。\n- 最终触发 \"Kernel panic - not syncing: Fatal exception in interrupt\"。\n- 在 6.12.18 版本中，崩溃前出现 \"psi: inconsistent task state\" 告警。",
  "root_cause": "内核在函数 `dequeue_rt_stack` (kernel/sched/rt.c:1035) 中触发了 `BUG()` 断言失败。日志中出现的 \"psi: inconsistent task state\" 表明调度器内部任务状态记录不一致，导致实时调度器在入队/出队操作时触发 panic。具体触发条件与代码缺陷尚在分析中。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "PREEMPT_RT内核调度器dequeue_rt_stack触发kernel BUG导致panic.txt",
  "content": "内核版本：6.12.13-talos, 6.12.18-mxie (PREEMPT_RT)\n\n## 问题现象\n- 系统运行 PREEMPT_RT 内核时每天随机崩溃一到两次。\n- 日志显示 \"kernel BUG at kernel/sched/rt.c:1035!\"，伴随 \"Oops: invalid opcode\"。\n- Call Trace 显示崩溃路径为：dequeue_rt_stack -> enqueue_task_rt -> enqueue_task -> try_to_wake_up。\n- 最终触发 \"Kernel panic - not syncing: Fatal exception in interrupt\"。\n- 在 6.12.18 版本中，崩溃前出现 \"psi: inconsistent task state\" 告警。\n\n## 问题根因\n内核在函数 `dequeue_rt_stack` (kernel/sched/rt.c:1035) 中触发了 `BUG()` 断言失败。日志中出现的 \"psi: inconsistent task state\" 表明调度器内部任务状态记录不一致，导致实时调度器在入队/出队操作时触发 panic。具体触发条件与代码缺陷尚在分析中。\n\n## 解决方案\n暂无"
}
