{
  "title": "xfs_db 进程在 ppc64le 平台执行 xfs/442 测试时挂起",
  "kernel_version": "5.2.0-rc4+ (xfs-linux xfs-5.3-merge-10)",
  "phenomenon": "- 在 ppc64le 架构上运行 xfstests 测试集的 xfs/442 用例时，xfs_db 进程挂起约 24 小时无响应。\n- 通过 SysRq 查看阻塞状态，发现进程处于 D 状态。\n- 堆栈信息显示进程卡在 `wait_on_page_bit_killable`，调用链为：`handle_page_fault` -> `do_page_fault` -> `handle_mm_fault` -> `do_swap_page` -> `wait_on_page_bit_killable`。\n- 该问题在 ppc64le 上复现两次，在 x86_64 上未复现。",
  "root_cause": "未明确（Bug 状态为 NEW，尚无开发者分析结论）",
  "solution": "未明确",
  "category": "kernel",
  "source_file": "xfs_db 进程在 ppc64le 平台执行 xfs_442 测试时挂起.txt",
  "content": "内核版本：5.2.0-rc4+ (xfs-linux xfs-5.3-merge-10)\n\n## 问题现象\n- 在 ppc64le 架构上运行 xfstests 测试集的 xfs/442 用例时，xfs_db 进程挂起约 24 小时无响应。\n- 通过 SysRq 查看阻塞状态，发现进程处于 D 状态。\n- 堆栈信息显示进程卡在 `wait_on_page_bit_killable`，调用链为：`handle_page_fault` -> `do_page_fault` -> `handle_mm_fault` -> `do_swap_page` -> `wait_on_page_bit_killable`。\n- 该问题在 ppc64le 上复现两次，在 x86_64 上未复现。\n\n## 问题根因\n未明确（Bug 状态为 NEW，尚无开发者分析结论）\n\n## 解决方案\n未明确"
}
