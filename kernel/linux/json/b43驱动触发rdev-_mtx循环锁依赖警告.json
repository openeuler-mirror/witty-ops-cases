{
  "title": "b43驱动触发rdev->mtx循环锁依赖警告",
  "kernel_version": "3.7.4",
  "phenomenon": "- 内核日志报告 \"possible circular locking dependency detected\"\n- 进程 hostapd 在执行操作时触发锁验证器警告\n- 涉及锁对象：`rdev->mtx`、`cfg80211_mutex`、`(&wl->firmware_load)`\n- 系统提示可能发生 DEADLOCK",
  "root_cause": "b43 驱动在移除接口（调用链 `nl80211_del_interface` -> `b43_op_stop` -> `cancel_work_sync`）过程中，持有 `rdev->mtx` 锁并尝试获取 `(&wl->firmware_load)` 锁。而在另一条路径（`wiphy_register`）中，存在 `(&wl->firmware_load)` -> `cfg80211_mutex` -> `rdev->mtx` 的锁获取顺序，从而形成了 `rdev->mtx` -> `(&wl->firmware_load)` -> `cfg80211_mutex` -> `rdev->mtx` 的循环依赖。",
  "solution": "暂无",
  "category": "kernel",
  "source_file": "b43驱动触发rdev-_mtx循环锁依赖警告.txt",
  "content": "内核版本：3.7.4\n\n## 问题现象\n- 内核日志报告 \"possible circular locking dependency detected\"\n- 进程 hostapd 在执行操作时触发锁验证器警告\n- 涉及锁对象：`rdev->mtx`、`cfg80211_mutex`、`(&wl->firmware_load)`\n- 系统提示可能发生 DEADLOCK\n\n## 问题根因\nb43 驱动在移除接口（调用链 `nl80211_del_interface` -> `b43_op_stop` -> `cancel_work_sync`）过程中，持有 `rdev->mtx` 锁并尝试获取 `(&wl->firmware_load)` 锁。而在另一条路径（`wiphy_register`）中，存在 `(&wl->firmware_load)` -> `cfg80211_mutex` -> `rdev->mtx` 的锁获取顺序，从而形成了 `rdev->mtx` -> `(&wl->firmware_load)` -> `cfg80211_mutex` -> `rdev->mtx` 的循环依赖。\n\n## 解决方案\n暂无"
}
