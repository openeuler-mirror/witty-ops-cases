#标题
系统从挂起恢复时libata-eh触发内核WARNING

#内核版本
5.17.4-200.fc35.x86_64

#问题现象
- 系统从挂起状态恢复时触发内核 WARNING。
- 日志显示：`WARNING: CPU: 0 PID: 97 at drivers/ata/libata-eh.c:3956 ata_scsi_port_error_handler+0x803/0x830`。
- 调用栈包含 `ata_scsi_error`、`scsi_error_handler`。

#问题根因
未明确。问题触发于系统从挂起恢复过程中，具体位于 `drivers/ata/libata-eh.c` 文件的 `ata_scsi_port_error_handler` 函数。

#解决方案
暂无。开发者建议尝试最新主线内核验证问题是否仍存在。
