# <模块名> 分析报告

> 本模板为单模块报告结构。生成报告前先读，严格按此结构填充。所有 `<...>` 占位符替换为实际内容。各模块的数据源/排除规则见 `module-<模块>.md`。

## 报告头部（必填）

- **报告生成时间**：YYYY-MM-DD
- **流水线名称**：ALL-OS-update-<流水线简称>
- **源版本/架构**：<version> / <arch>
- **执行时间**：<来自 CSV 或流水线元数据>
- **执行状态**：success / failed
- **数据完整性摘要**：
  - `all_logs.tar`：✅ 正常 / ⚠️ 缺失或空 / ❌ 下载失败
  - `update-results.csv`：✅ / ⚠️ / ❌
  - `<模块>.tar` 或 `<模块>.log`：✅ / ⚠️ / ❌
  - （kernel 额外列 `ltp.tar`、`mugen-oeupdate-results`）

> 数据完整性摘要可写成列表或表格。某文件不存在时在该模块报告里记录原因并跳过对应分析。

## 原始统计

展示该模块的原始统计文件内容（CSV 行 / *.log 段落），用 ``` 代码块包裹原始文本。

对每个版本展示：
- CSV 原始数据行
- 该模块日志（pkgmanage.log / pkgserver.log / pkgcmd.log / ltp.log 等）的原始片段
- 排除规则执行情况（pkgserver/pkgcmd 等有排除项的模块）：表格列出被排除用例与排除依据

> kernel 模块若 CSV 无 kernel 行且 all_logs 无 kernel 目录且 ltp 为空 → 直接跳到"分析结论"记录"本次流水线未测试 kernel 模块"，结束本报告。

## 拦截概览（必填表格）

| 指标 | 数量 | 占比 |
|---|---|---|
| 总失败用例（排除后） | N | 100% |
| 跳过用例 | x | — |
| ✅ 自动拦截 | x | x% |
|   — 非问题(环境/预期行为) | x | |
|   — 已知问题(案例库命中) | x | |
|   — 已有关联分析(连锁同根因) | x | |
| ⚠️ 待确认(部分匹配) | x | x% |
| ❌ 未拦截(需人工分析) | x | x% |
| **拦截率** | **x/N** | **x%** |

> 表格下方可加一句说明（如"全部为容器环境限制，非包质量问题"）。拦截率 = (✅自动拦截 + ⚠️待确认) / 总失败用例。

## ✅ 自动拦截用例

命中案例库且定性明确（是否问题=否 或 是）的用例。可用表格或逐条列出。

### <用例名>（或聚类标题）
- 拦截类型：非问题(环境/预期行为) / 已知问题(案例库命中) / 已有关联分析(连锁同根因)
- 匹配案例：[案例文件名]（用相对链接指向案例库文件）
- 匹配置信度：高 / 中 / 低
- 根因：一句话根因
- 关键日志：`报错日志截取关键前后 5 行`

> 同根因连锁失败的多个用例可合并为一条，列明涉及用例与合并理由。跨模块同根因可引用其他模块报告。

## ⚠️ 待确认用例

命中案例库但案例标注"是否问题: 待确认"的用例，逐条列出。

### <用例名>
- 部分匹配案例：[案例文件名] (相似度: 高/中/低)
- 检索关键词：`<用过的检索关键词>` → 命中案例
- 案例状态：是否问题=待确认
- AI 初步根因：根据日志特征推断的根因
- 不确定点：案例未最终定性，标注不确定之处
- 关键日志证据：```报错日志关键片段```
- 需人工确认：列出需要人工拍板的问题

## ❌ 未拦截用例

无案例匹配的用例，**每个用例必须完整给出以下 6 个字段 + 案例草稿**。同根因用例可合并为一个聚类，但聚类内列明所有涉及用例。

### <用例名>

**测试执行上下文**：
- 所属测试套/包：<package>
- mugen 日志路径：`all_logs/.../oe_test_<name>/<timestamp>.log`（或 ltp 路径）
- 测试脚本生命周期阶段：run_test / config / cleanup（如 test_execution → test_restart）

**关键日志证据**：
```
报错日志截取关键前后 5 行
```

**分析逻辑链**：
1. 第一步……
2. 第二步……
（逐步推演失败发生过程）

**检索记录**：
- 关键词1: `<词>` → 案例 <文件名> / 无匹配
- 关键词2: `<词>` → 无匹配

**根因推断**：一句话根因（环境限制 / 版本差异 / 真 bug），并说明是否与包更新质量相关。

**修复建议**：具体建议（跳过该用例 / 修测试预期 / 提 issue / 检查环境配置）。非问题的标注"非问题"。

**📥 案例草稿**（人工审核后入库）：
- 建议文件名：`<模块>-<现象简述>.md`
- 入库路径：`witty-ops-cases/openEuler_test/update_test/<模块>/`

> 案例草稿用 blockquote 引用块包裹，格式与案例库 `.md` 一致：包含
> `# 标题`、`## 现象`、`## 基本信息`(测试用例/系统版本/是否问题/关联issue)、
> `## 错误日志`(``` ```)、`## 根因`、`## 影响范围`、`## 解决方案`、`## 关键词`。

## 案例库缺口分析（必填表格）

| 根因聚类 | 未拦截用例数 | 涉及用例 | 案例库现状 | 建议优先级 |
|---|---|---|---|---|
| <聚类名> | x | oe_test_... | 0 条 / 有草稿待确认 | 🔴高 / 🟡中 / 🟢低 |

**下一步建议**：按投入回报排序（补 1 条案例能拦截几个用例），给出优先级最高的补案例动作。
